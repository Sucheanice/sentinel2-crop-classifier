# 地块边界自动提取方案

> **记录日期**：2026-07-30
> **背景**：当前方案A用已有确权SHP做地块边界+作物识别。新区县若无地块SHP，需先从DOM自动提取边界，再套方案A。

---

## 一、当前能力边界

```
方案A 实际做的事：
  地块边界SHP（外部输入） + S2多期多光谱 → 作物识别
                          ↑
                    我们只解决了这部分

方案A 不涉及的部分：
  地块边界 SHP 是从哪里来的？ ← 确权/普查数据，外部给定
```

---

## 二、目标：无地块SHP的完整流程

```
新区县 DOM 影像（1m, RGB） 
    │
    ▼
地块边界自动提取模型（U-Net / DeepLabV3+）
    │
    ▼
地块边界 SHP
    │
    ▼
方案A：S2多期多光谱 + 地块边界SHP → 作物识别
    │
    ▼
带作物标签的地块 SHP
```

---

## 三、边界提取模型训练方案

### 3.1 训练数据准备

| 要素 | 来源 | 说明 |
|------|------|------|
| 输入 | DOM 影像（1m, 3波段 RGB） | 安居/射洪已有的 DOM |
| 标签 | result.shp 栅格化 | 边界=1, 内部=0, 外部=0 |
| 样本量 | 安居 15,273 地块 + 射洪 31,075 地块 | 标注量足够 |

### 3.2 模型选择

| 模型 | 说明 |
|------|------|
| U-Net | 经典语义分割，边界/边缘检测常用 |
| DeepLabV3+ | 更强的多尺度特征，适合复杂地形 |
| SAM (Segment Anything Model) | Meta开源大模型，可做零样本分割，需finetune适配农田场景 |

推荐先用 **U-Net** 快速验证，再用 DeepLabV3+ 或 SAM 提升精度。

### 3.3 后处理

语义分割输出 → 边界概率图 → 二值化 → 骨架化 → 多边形矢量化 → SHP

---

## 四、限制与风险

| 限制 | 说明 |
|------|------|
| 只用 RGB 三波段 | 无 NIR，边界与水体/阴影/裸土易混淆 |
| S2 10m 不能用于边界提取 | 田埂通常 1-3m 宽，10m 像素里边界和作物混合 |
| 跨区县泛化 | 四川盆地丘陵区地块形状差异大，可能需逐区 finetune |
| 不是端到端 | 边界提取 + 作物识别是两段式流程，不能一步到位 |

---

## 五、为什么1m DOM不能直接做作物识别

| 条件 | 方案A需要 | 1m DOM提供 | 可行？ |
|------|----------|-----------|--------|
| 光谱波段 | 7波段 (B02/B03/B04/B08/B8A/B11/B12) | 3波段 (RGB) | ❌ |
| 时间序列 | 4期（5-8月物候变化） | 单期快照 | ❌ |
| 植被指数 | NDVI/EVI/NDWI/SAVI需要NIR/SWIR | 无NIR/SWIR | ❌ |
| 空间分辨率 | 10m/20m | 1m | 不匹配 |

**DOM管"地在哪"，S2管"种了什么"，分工不同。**

---

## 六、执行路径（待实施）

```
第一步：准备训练数据
  ├── 安居区 DOM + result.shp → 边界标签
  └── 射洪市 DOM + result.shp → 边界标签

第二步：训练边界提取模型
  ├── U-Net 快速验证
  ├── 评估：边界IoU、地块召回率
  └── 可选：DeepLabV3+/SAM 提升精度

第三步：新区县推理
  ├── 新区县 DOM → 边界提取模型 → 地块边界 SHP
  └── 地块边界 SHP + S2多期影像 → 方案A → 作物识别

第四步：精度评估
  └── 与人工勾绘边界对比
```

---

## 七、相关文件

- 方案A 实现：`predict_parcel.py`
- 全流程训练/预测：`pipeline_v2.py`
- 成果质量分析：`成果质量问题与解决方案_20260729.md`
- 影像数据选择审查：`影像数据选择审查与改进建议_20260729.md`

---

*本方案待下一步实施，当前优先完成方案A的优化（类别均衡、云量问题等）。*
