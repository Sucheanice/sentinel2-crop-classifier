# 成果质量问题分析与解决方案

> **分析日期**：2026-07-29
> **对比数据**：
> - 模型输出：`待训练数据6_anju/crop_polygons_anju_1m.shp`
> - 人工真值：`待训练数据5/遂宁市安居区成果数据 (11)/result.shp`

---

## 一、数据对比发现

| 指标 | 模型输出 | 人工真值 | 差距 |
|------|----------|----------|------|
| 多边形数 | 2,408 | 15,273 | 模型仅为真值的 16% |
| 总面积 | 6.15 km² | 45.94 km² | 模型仅为真值的 **13%** |
| 最大多边形 | 1,349,111 m²（~1.35 km²） | 正常地块级 | **异常** |
| 最小多边形 | 40 m² | ~333 m²（0.5 亩） | 噪声碎片 |
| 作物类别 | corn / rice（2 类） | 玉米 / 水稻（2 类） | 一致 |

**结论**：模型输出覆盖严重不足（仅 13%），且多边形质量差（碎块 + 异常大块）。

---

## 二、5 个问题的根因分析

### 问题 1：锯齿

**现象**：SHP 多边形边缘呈明显阶梯状（锯齿形）。

**根因**：当前流程是 `10m 像素 → 直接矢量化为多边形`，10m 像素的方形边缘天然产生阶梯。重采样到 1m 后，每个 10m 像素变成 10×10=100 个相同值的 1m 像素，锯齿不变，只是密度增加 100 倍。

**涉及代码**：`raster_to_shp.py` 的 `rasterio.features.shapes()` 直接按像素栅格化边界，无平滑处理。

---

### 问题 2：玉米和水稻混种

**现象**：同一块田里出现玉米和水稻交替的小碎片。

**根因**：**逐像素预测**。模型对每个 10m 像素独立判断作物类型。同一块地里边缘像素和内部像素的光谱略有差异（尤其是混云、阴影、土壤背景），导致相邻像素预测出不同作物，矢量化的多边形自然碎片化。

**深层原因**：模型**训练时用地块均值特征**（`pipeline_v2.py:174-182`，一个地块 8 波段 × 4 期 = 32 维均值），但**预测时用逐像素特征**。这是 **train/serve skew（训练-推理不一致）**，等于模型学了"地块均值→作物"，推理时却喂"单像素特征"。

---

### 问题 3：空白田地没有识别

**现象**：真值有作物的地块，模型输出为空白（nodata=0）。

**根因（两个）**：

1. **WorldCover 掩膜过严**：`mask_cropland.py` / `resample_to_dom.py` 用 ESA WorldCover 2021 年的耕地分类（class=40）做掩膜。WorldCover 是 **2021 年**的数据，但项目预测的是 **2025 年**——4 年间可能有撂荒地复耕、新开垦农田等变化，2021 年的耕地图无法覆盖。非耕地的像素直接被置 0。

2. **云遮挡**：4 期影像中只要有 1 期某像素被云覆盖，该像素在该期的特征值为 nodata。多期叠加后，如果某些区域在多期都受云影响，模型输入特征缺失，输出为 0。

---

### 问题 4：明明有作物却没识别

**现象**：目视 DOM 上明显有绿色作物，但模型没识别出来。

**根因**：与问题 3 叠加，额外因素：
- 逐像素预测时，模型对单像素光谱的判别力弱于地块均值（训练时没见过单像素特征）
- 建筑区边缘、田埂、小路等非耕地像素混入，模型倾向于预测背景类

---

### 问题 5：过度识别（范围过大）

**现象**：出现 1.35 km² 的巨大多边形，远超正常地块面积。

**根因**：**无地块边界约束**。当相邻的多个地块都被预测为同一种作物时，`rasterio.features.shapes()` 会把它们合并成一个大多边形（因为像素值相同、空间连通）。真值里这些是独立地块（不同农户、不同种植时间），但模型不区分边界。

---

## 三、核心问题总结

**5 个问题有一个共同根因**：

> 当前流程是 `逐像素分类 → 直接栅格转矢量`，**完全没有使用地块边界**。

而真值数据 `result.shp` 本身就是**带精确边界的地块级数据**，有 15,273 个独立地块。正确的做法应该是：**用地块边界约束预测，每个地块输出一个作物类型**。

---

## 四、解决方案

### 方案 A（推荐）：基于地块边界的对象级分类

**思路**：既然有地块边界数据（真值 SHP / 确权数据），就不应该逐像素分类再矢量化，而应该**以地块为单位**进行分类。

#### 流程对比

```
当前流程（有问题）：
S2 10m → 逐像素预测 → 栅格 → 矢量化（锯齿+碎片+大块）

改进流程（方案A）：
S2 10m + 地块边界 SHP → 每个地块提取 zonal mean 特征 → 模型预测
→ 每个地块赋一个作物类型 → 输出 SHP（边界完美、无锯齿）
```

#### 具体步骤

1. **输入地块边界 SHP**（确权/普查数据，或人工勾绘）
   - 如果有：直接用 `result.shp` 的地块边界
   - 如果没有新区县的 SHP：用 1m DOM 影像做地块分割（图像分割 + 边界提取）

2. **逐地块提取特征**（与训练方式一致）
   - 对每个地块多边形，在 S2 影像上做 zonal mean（`rasterio.mask.mask` + `np.nanmean`）
   - 每个地块得到 32 维特征（4 期 × 8 特征），与训练特征完全一致
   - **消除 train/serve skew**

3. **逐地块预测**
   - `model.predict(parcel_features)` → 每个地块一个作物类型
   - 不再有碎片、混种、大块问题

4. **输出 SHP**
   - 直接输出地块多边形 + crop 属性
   - 边界来自原始 SHP，**天然无锯齿**
   - 每个地块一个值，**天然无混种、无大块**

#### 代码改动

核心改动在 `pipeline_v2.py` 的 `step3_predict`，从"逐像素分块预测"改为"逐地块 zonal 预测"：

```python
def step3_predict_parcel(model, bundle, s2_dirs, parcel_shp, out_shp):
    """基于地块边界的预测：消除锯齿、碎片、混种、大块"""
    import geopandas as gpd
    from rasterio.mask import mask

    gdf = gpd.read_file(parcel_shp)
    # 栅格化到 S2 grid 提取 zonal mean（复用 step2 的 extract 逻辑）

    features = []
    for _, row in gdf.iterrows():
        geom = row.geometry
        parcel_feats = extract_zonal_mean(geom, s2_dirs)  # 与训练一致
        features.append(parcel_feats)

    X = np.array(features)
    X_selected = X[:, bundle['selected_features']]
    y_pred = model.predict(X_selected)

    gdf['crop'] = [bundle['class_names'][i] for i in y_pred]
    gdf.to_file(out_shp)
```

#### 优势

| 问题 | 是否解决 | 原因 |
|------|----------|------|
| 锯齿 | ✅ 彻底解决 | 边界来自 SHP 矢量，非像素栅格化 |
| 混种 | ✅ 彻底解决 | 一个地块一个作物类型 |
| 空白田地 | ✅ 解决 | 每个地块都预测，不会被掩膜掉 |
| 漏识别 | ✅ 大幅改善 | 消除 train/serve skew |
| 过度识别 | ✅ 彻底解决 | 地块边界天然隔离 |

#### 前提条件

- **必须有地块边界 SHP**：这是本方案的前提。对于已有确权数据的区域（如安居、射洪），直接用。对于新区县，需要先获取地块边界数据。

---

### 方案 B：保留逐像素预测，但后处理优化

**适用场景**：没有地块边界 SHP 时的备选方案。

#### B1. 消除锯齿

```python
# 矢量化后做多边形简化+平滑
# simplify 容差 5m 消除阶梯，shapely.buffer(0) 修复自交
gdf['geometry'] = gdf['geometry'].simplify(tolerance=5)
# 可选：Chaikin 算法平滑边界
```

#### B2. 消除混种碎片

```python
# 众数滤波（Majority Filter）：用 3×3 窗口对分类栅格做众数滤波
# 消除孤立像素，再矢量化
from scipy.ndimage import median_filter
filtered = median_filter(crop_map, size=3)
```

#### B3. 修复过度识别大块

```python
# 矢量化后按面积过滤：
# - 面积 > 50000 m²（50 亩）的多边形拆分或标记为可疑
# - 面积 < 100 m² 的碎片删除
gdf = gdf[gdf.geometry.area > 100]      # 去碎片
gdf = gdf[gdf.geometry.area < 50000]     # 去大块（或单独处理）
```

#### B4. 修复 WorldCover 掩膜过严

```python
# 不用 WorldCover 2021 硬掩膜，改为：
# 1. 不掩膜，让模型预测所有像素
# 2. 或用 NDVI > 0.2 作为软掩膜（有植被才预测作物）
```

#### 劣势

- 治标不治本：混种碎片只是被"糊掉"，不是真正消除
- 锯齿只是平滑，不会变成真正的田埂边界
- 大块拆分没有依据（不知道该在哪拆）
- 总体精度提升有限

---

### 方案 C：混合方案（A + B 结合）

**最务实方案**：

1. **有地块边界的区域**：用方案 A（对象级分类）
2. **无地块边界的区域**：用方案 B（逐像素 + 后处理）
3. **新增区域**：先从 DOM 影像自动提取地块边界（图像分割），再用方案 A

---

## 五、推荐执行路径

```
第一步：立即用方案 A 重做安居区
  ├── 输入：result.shp 的 15,273 个地块边界 + S2 影像
  ├── 提取：每地块 zonal mean 特征（与训练完全一致）
  ├── 预测：model.predict() → 每地块一个作物类型
  ├── 输出：crop_parcels_anju.shp
  └── 对比：与真值 result.shp 做精度评估（混淆矩阵）

第二步：对比评估
  ├── 方案 A 输出 vs 真值：混淆矩阵、IoU、面积误差
  ├── 与当前方案（逐像素）对比：哪个更接近真值？
  └── 量化 5 个问题的改善程度

第三步：如果方案 A 效果好，推广到射洪和新区域
  ├── 射洪：用 result.shp 地块边界 + 射洪模型
  ├── 新区域（有确权 SHP）：直接用方案 A
  └── 新区域（无 SHP）：先做 DOM 地块分割，再用方案 A
```

---

## 六、关键结论

| 维度 | 当前方案（逐像素） | 推荐方案（地块级） |
|------|-------------------|-------------------|
| 训练/推理一致性 | ❌ 不一致（训练用均值，推理用单像素） | ✅ 一致（都用地块均值） |
| 边界质量 | ❌ 锯齿严重 | ✅ 矢量边界无锯齿 |
| 混种碎片 | ❌ 大量 | ✅ 一个地块一个类型 |
| 覆盖率 | ❌ 仅 13% | ✅ 100%（每个地块都预测） |
| 过度识别 | ❌ 有大块合并 | ✅ 地块天然隔离 |
| 需要地块 SHP | 不需要 | **必须有**（当前已有） |

**核心建议**：切换到方案 A（基于地块边界的对象级分类），这是解决全部 5 个问题的根本方法。当前已有 `result.shp` 地块边界数据，可以立即实施。

---

*分析完毕，建议优先执行方案 A 的第一步——用已有地块边界重做安居区预测并评估。*
