# 工作记录 — 大春水稻/玉米二分类（23县库训练 + 绵阳迁移）

**日期**: 2026-08-14

---

## 一、任务背景

大春作物识别（水稻 vs 玉米）需要扩展到**第一批次任务区 = 绵阳 6 县**（梓潼/盐亭/三台/平武/江油）。但绵阳本地承保标注不一定有（即使有可能不全），因此采用**「23 县标注库训练 + 绵阳迁移」**路线。

大春标注库（23 县，泸州/遂宁/雅安/宜宾）与绵阳任务区**区域不重叠**，本质又是一次跨区迁移，但比果园迁移更站得住：水稻/玉米靠物候+水分信号（水田淹水、返青/分蘖），比"果园靠地形"普适；且大春特征工程本就不用 DEM，避开了果园被 DEM 主导的坑。

---

## 二、本次确定的关键决策

| 决策点 | 结论 |
|--------|------|
| 分类类别 | **只做「水稻 vs 玉米」二分类** |
| 高粱 | 从标签中当"其他/剔除"处理，不单独建类（绵阳任务清单里无高粱；高粱仅 2,154 块且可能集中在射洪） |
| 果园 | 暂用叙永模型，按"去 DEM + 套后处理"方案执行（待落地） |
| 训练路线 | 23 县库训练 + 绵阳迁移（绵阳标注不全） |
| 下载/训练位置 | 服务器（10.62.11.15） |
| 时相策略 | 选 **A**：每月取云量最低一景（先跑通验证，牺牲部分跨瓦片时相对齐） |

---

## 三、影像缺口盘点（08-14）

### 3.1 区域与瓦片

| 侧 | 区域 | 瓦片数 | 说明 |
|----|------|--------|------|
| 训练侧 | 23县库（实际 21 区县：遂宁5/泸州7/雅安6/宜宾3） | **25** | 范围 [102.55, 27.69, 106.23, 31.16] |
| 推理侧 | 绵阳 6 县 | **12** | 平武/江油北部落在 S 带 |
| 去重后 | — | **31 瓦片 × 4 时相 ≈ 124 景** | 6 个 R 带瓦片两侧重复 |

### 3.2 类别分布（大春标注库，90,301 块）

玉米 52,564 / 水稻 34,957 / 高粱 2,154 / 果树 527 / 经济林 61 / 蔬菜 38。

→ 二分类只保留玉米+水稻 = **87,521 块**。

### 3.3 时相对齐情况（A 方案）

- 5 月：全部 05-20（天然对齐，云量低）
- 8 月：多数 08-03；S 带 08-13/08-31
- 6 月：06-11/12/13 为主，个别 06-14/06-26（云量高）
- 7 月：07-12 ~ 07-31 散落（约 3 周跨度）

> 隐患：6/7 月跨瓦片日期不齐，影响时序/物候特征对齐；若后续绵阳迁移精度不达标且定位为时相错位，只需对少数瓦片补下锚点日期（06-12/07-16），无需全量重下。

### 3.4 已本地可跳过

- 遂宁 `48RWU`/`48RWV`（4 期，即 `待训练数据6`）
- 江油 `48SWA`/`48SVA`（部分）

---

## 四、产出文件（08-14）

| 文件 | 说明 |
|------|------|
| `哨兵影像下载地址.txt` | **纯链接下载清单**（区域→瓦片→时相分组，10 波段/景） |
| `哨兵影像下载清单_大春.csv` | 机器可读完整下载链接（含 scene_id/云量/10 波段 URL） |
| `哨兵影像下载清单_大春.md` | 汇总表（瓦片×日期×云量×scene_id） |
| `_tile_inventory.py` | 瓦片盘点脚本（读标注库范围 + 绵阳县界算 MGRS） |
| `_gen_s2_links.py` | STAC 检索 + 生成 CSV/MD 下载清单 |
| `_gen_txt.py` | CSV → TXT 纯链接 |
| `_test_stac.py` | STAC 直连验证 |

数据源：Earth Search STAC (element84) → COG `sentinel-cogs.s3.us-west-2.amazonaws.com`（服务器需能访问 us-west-2，必要时挂代理）。

---

## 五、后续流水线（6 步）

| 步 | 内容 | 依赖 |
|----|------|------|
| 0 | 标签清洗：剔除高粱/果树/经济林/蔬菜，只留水稻+玉米 | 无（现在可做） |
| 1 | 影像就位（下载中） | 影像 |
| 2 | 特征工程：复用 `common.py` 5 层特征（10 波段） | 影像+标签 |
| 3 | 训练集构建 + **空间分组 / 留一市验证** | 标签 |
| 4 | 训练 LightGBM 二分类 | 特征 |
| 5 | 绵阳迁移 + 验证（江油前进村 GDB 为唯一本地标注） | 模型 |
| 6 | 输出 + 后处理（地块平滑/去碎斑） | 推理 |

**关键验证**：留一市验证（留雅安/宜宾当测试）是判断"23县训练→绵阳迁移"可行性的核心证据；绵阳本地精度只能靠江油前进村 GDB + 人工抽查。

---

## 六、本次会话进度

- [x] 影像缺口盘点 + 下载链接生成
- [x] 标签清洗（水稻+玉米二分类干净标签）
- [x] 训练脚本改造（二分类 + 空间分组验证）
- [x] 用遂宁现有影像端到端验证
- [x] 23 县全量特征提取 + 训练 + 绵阳迁移（已于第八章完成：留一县 89.9%、绵阳迁移 83.3%）

---

## 七、遂宁端到端验证结果（08-14）

### 7.1 标签清洗

`_label_clean.py` → `待训练数据大春\大春标注_水稻玉米.gpkg`

- 原 90,301 块 → 清洗后 **87,521 块**（玉米 52,564 / 水稻 34,957）
- 剔除：高粱 2,154 / 果树 527 / 经济林 61 / 蔬菜 38
- 字段确认：`SZMC`（市）、`QXMC`（县）、`TBMJ`（面积）、`类别`、`geometry`，CRS=EPSG:4326

### 7.2 训练脚本改造

新增 `train_dachun_binary.py`：二分类 objective='binary' + **空间分组验证**（GroupKFold + LeaveOneGroupOut），替代 v3 的随机 hold-out。

### 7.3 遂宁 5 区验证（features_5districts.csv，29,781 样本 → 预处理后 22,823）

| 验证方式 | Acc | F1 |
|----------|-----|-----|
| GroupKFold（按区县分组，5折） | **0.9381 ± 0.0288** | 0.9375 ± 0.0295 |
| 留一县（平均） | **0.9381** | — |

留一县明细：

| 留出区县 | Acc | n |
|----------|-----|---|
| 大英县 | 0.9581 | 6,062 |
| 安居区 | 0.8940 | 14,460 |
| 射洪市 | 0.9785 | 279 |
| 船山区 | 0.9251 | 641 |
| 蓬溪县 | 0.9348 | 1,381 |

### 7.4 关键结论

1. **空间分组验证与 v3 随机划分结果几乎一致（93.81% vs 93.58%）**，说明模型在遂宁 5 区内**空间可泛化**，不是靠同区域泄漏刷分。
2. **Top 特征以 B11（SWIR 短波红外）+ NDWI 为主**（`TSTAT_B11_mean`、`2025-07-16_B11`、`DELTA_NDWI`），说明模型学的是**水分/物候信号**（水稻淹水 vs 玉米旱地），不是 DEM——这正是跨区迁移想要的方向，与果园被 DEM 主导形成鲜明对比。
3. 安居区留出时精度最低（89.4%），提示区内农田光谱有局部差异，迁移到绵阳时要有精度回落的预期。

> 待办：23 县影像下载完成后，生成 23 县特征 CSV（10 波段），用同一脚本按 `QXMC/SZMC` 做留一市验证，再训练全量模型 → 绵阳迁移。

---

## 八、23 县全量训练 + 绵阳迁移 + 前进村/安居区预测（08-15 ~ 08-18）

### 8.1 影像下载/就位

这两天补下并验证了推理侧影像：

| 用途 | 瓦片 | 说明 |
|------|------|------|
| 绵阳迁移 | 48RVV / 48RWV / 48SVA 等 4 瓦片 | 绵阳标注 50607 块全部分配到 4 瓦片，未覆盖 0 |
| 前进村（江油） | 48SVA / 48SWA | 4 期 × 10 波段齐全 |
| 安居区（遂宁） | 48RWU / 48RVU | 4 期 × 10 波段齐全；48RVU 的 B02 86% 无效零值（仅西边 1.8km 窄条，可忽略） |

> 48RUT / 48RVU 四个时相 B02 缺失，但不影响上述三类推理。

### 8.2 23 县全量训练

- 标签清洗后 **87,521 块**（玉米 52,564 / 水稻 34,957）；县名归一化「大英市 → 大英县」。
- 预处理后 **68,083 样本 / 20 县**。
- GroupKFold（按区县分组）：**Acc 0.874 ± 0.039，F1 0.871 ± 0.043**；留一县平均 **Acc 0.899**。
- 模型保存 `dachun_binary_23counties/crop_model_binary.pkl`（LightGBM，172 维 → Top 40，类别 水稻/玉米）。

### 8.3 推理侧两个 bug（关键修复）

`predict_dachun_binary.py` 初版用「栅格化整地块均值」替代训练侧「点采样」，本应等价，但有两处 bug，导致安居区精度从 0.89 腰斩到 0.55：

1. **栅格化 label 用枚举位置 `i+1`**：遇到空几何/退化 buffer 被 `continue` 跳过后，label 与地块错位 → 后续地块特征全部张冠李戴（精度腰斩主因）。
2. **`buffer(-5)` 内缩**丢弃小地块 → 特征全 NaN 被填 0，全误判为水稻。

修复：连续 label 编号 + 不缓冲（`BUFFER_DIST=0`、`MIN_PX=1`）。修复后安居区推理 0.8715，与训练 CSV 自洽 0.8725 一致。

### 8.4 三类推理结果

| 任务 | 指标 | 结果 |
|------|------|------|
| 绵阳迁移验证（50607 块有标签） | Acc / F1 | **0.8331 / 0.8301** |
| 前进村地块级预测（386 块耕地） | 分布 | 水稻 271 / 玉米 115 |
| 安居区地块级预测（15044 块） | Acc / F1 | **0.8715 / 0.8712** |

绵阳分县 Acc：盐亭 0.929 / 平武 0.921 / 三台 0.887 / 梓潼 0.804 / **江油 0.677**（江油明显偏低）。

### 8.5 安居区全图盲预测（不依赖地块边界）

按 `技术流程说明.md` 第 5 节「逐像素预测 → 分类栅格」，验证模型能否脱离标注边界自主识别。

- 输出分类栅格 `anju_cropmap_48RWU.tif`，矢量化 `anju_cropmap.shp`（EPSG:4523，套合 DOM）。
- **盲预测 vs 标注地块对比：Acc 0.8696，与地块级 0.8715 几乎一致，两者一致率 97.52%**。
- 结论：模型不靠田块边界作弊，去掉边界识别能力不掉。玉米召回 90.6%，水稻召回 83.2%，NDVI 掩膜仅误杀 1.01%。

### 8.6 前进村全图盲预测 + 时相对齐问题

前进村 `前进村.img` 是 0.5m RGB DOM（3 波段），不能用 S2 模型直接预测；盲预测仍用 Sentinel-2（48SWA 标准瓦片，前进村 easting≈504km）。

- 48SWA 盲预测 vs 地类标注（水田→水稻、旱地→玉米，n=376）：**地类一致率 67.3%**，玉米识别 86.7%，**水稻识别仅 5.6%**（90 块水田只判对 5 块）。
- 相邻 48SVA 结果：一致率 22.9%，未识别率 54.5%（不可用）。

**根因排查（关键结论）**：

1. 前进村标准瓦片 48SWA 的 6 月影像是 **06-04，云量 92.72%**。
2. STAC 检索确认：**48SWA 2025 年 6 月 7 景云量全部 92%~100%，06-12 不存在，无低云可补**。
3. 因此前进村水稻识别失败是「6 月影像被云全覆盖」的客观数据限制，不是模型或单纯日期偏早问题。

### 8.6 补充：补下 48SVA 06-12 的验证结论（08-18）

曾建议「用 48SVA 06-12 替代 48SWA 6 月影像救回水稻」，补下后重跑盲预测验证，结论是**该建议不成立**：

- 48SVA（补 6 月后）盲预测 vs 标注：**一致率 22.87%，未识别 54.52%，与补前逐像素完全一致（一致率 1.0000），无任何改善**。
- 根因：**48SVA 的 06/07/08 三期影像实际只覆盖半幅**——有效 easting 分别只到 482.6 / 481.4 / 481.3 km，而前进村在 easting 501~506 km，**根本没被覆盖**；只有 48SVA-05 期覆盖到 509 km。
- 因此前进村的正确瓦片是 **48SWA**（四期完整覆盖 500~610 km），**不是 48SVA**；此前「质心全在 48SVA」的判断用的是影像标称边界（48SVA 名义 10980 列到 509 km），未发现 06/07/08 东半段为全空值。
- 结论：前进村水稻在当前哨兵数据下客观救不回（48SWA 6 月被云盖死、48SVA 又覆盖不到）。

### 8.7 关键结论

1. **哨兵影像的日期与云量都极其关键**：安居区时相对齐好（6/7/8 月低云）→ 水稻/玉米均 87% 左右；前进村 48SWA 6 月高云 → 水稻直接失真。
2. 模型学的是**水分/物候信号**（B11 + NDWI），对水稻识别依赖 6 月返青/淹水时相；该时相若被云遮挡，水稻特征丢失。
3. 前进村水稻要救回，原建议「用 48SVA 06-12 替代」已证伪（48SVA 06/07/08 只覆盖半幅、盖不到前进村，见 8.6 补充）。当前只能接受前进村只做玉米可用的现实，水稻需另找数据源（其他卫星 6 月影像或完整覆盖到 509 km 的 S1/S2 数据）。

### 8.8 产出文件（08-15~08-18）

| 文件 | 说明 |
|------|------|
| `dachun_binary_23counties/crop_model_binary.pkl` | 23 县二分类模型 |
| `predict_dachun_binary.py` | 统一推理脚本（mianyang/qianjincun/anju） |
| `predict_anju_fullmap.py` / `predict_qianjincun_fullmap.py` | 全图逐像素盲预测 |
| `vectorize_anju_cropmap.py` / `vectorize_qianjincun_cropmap.py` | 栅格矢量化成 shp |
| `dachun_prediction/mianyang_transfer.shp` | 绵阳迁移结果（含 true/correct） |
| `dachun_prediction/qianjincun_prediction.shp` | 前进村地块级预测 |
| `dachun_prediction/anju_prediction.shp` | 安居区地块级预测 |
| `dachun_prediction/anju_cropmap.shp` | 安居区全图盲预测矢量化（套合 DOM） |
| `dachun_prediction/qianjincun_cropmap.shp` | 前进村全图盲预测矢量化（套合 DOM） |
| `哨兵影像下载地址_48SWA补6月.txt` | 48SWA 6 月高云说明 + 48SVA 06-12 替代链接 |

### 8.9 影像波段/融合口径（S1/S2 + SVS 软件）

同事在 SVS 软件做「影像融合 + 坐标转换」时，问波段保留与波段组合怎么选，口径如下：

**波段序号约定**（SVS 里的 1/2/3/4 即影像的 4 个波段）：

| 序号 | 波段 | 波长 |
|------|------|------|
| 1 | 蓝 Blue | ~490nm |
| 2 | 绿 Green | ~560nm |
| 3 | 红 Red | ~665nm |
| 4 | 近红外 NIR | ~842nm |

（对应 Sentinel-2 的 B02 / B03 / B04 / B08，即 10m 的 4 个波段）

**波段组合选择**：

- **作物识别（推荐）→ 假彩色 `4(R)+3(G)+2(B)`**（近红外-红-绿），植被呈红色，水稻/玉米水旱差异最明显，是分类目视判读标准组合。
- 看自然色 → 真彩色 `3(R)+2(G)+1(B)`（红绿蓝），像普通航拍。
- 带 `+4波段` 的选项（如 `3R+2G+1B+4波段`）= RGB 显示用前三个，同时额外输出第 4 波段（近红外），供后续分类计算用；只出图可不带。

**S1/S2 波段保留口径**（与融合配套）：

- **S1（Sentinel-1 GRD）**：只保留 **VV、VH** 两个后向散射波段（`iw-vv.tiff` / `iw-vh.tiff`）；DN → dB 用 `10×log10(DN²)`，输出 float32。
- **S2（Sentinel-2 L2A）**：保留 **B02/B03/B04/B05/B06/B07/B08/B8A/B11/B12** 十个波段。
- S1 重投影/重采样到 S2 10m UTM 网格（双线性）对齐；S2 20m 波段上采样到 10m。
- **不降档**：分辨率不降（统一 10m）；位深不降到 uint8（后向散射/反射率会失真），保持 float32（最多 int16）；存档传输可转 COG 无损压缩。

### 8.10 待探讨：SVS 影像融合工作能否自研替代 / 是否可商业化（记录，暂不展开）

同事用国产商业软件 SVS 做「影像融合 + 坐标转换 + 波段组合」。讨论结论暂记如下，空了再深入：

- SVS 干的活本质是三件事：**波段组合（stack）、坐标转换（reproject）、融合成一张（mosaic/merge）**。
- 这些用 Python + `rasterio`/`GDAL` 是标准操作，项目内已在做同类事（S1→S2 重投影重采样、波段提取、输出 GeoTIFF），**技术上可自研替代**，且脚本化更利于批量/可复现/参数透明。
- **"卖钱"分三层**：① 内部跑通 ✅；② 当技术服务交付 ⚠️ 需对齐坐标精度/波段顺序/nodata/元数据/投影带（3度带 vs UTM）才算交付级；③ 当产品卖 ❌ SVS 卖的是"通用软件+售后"，与"定制脚本+服务"是两码事，不宜类比。
- **已知软肋**：S1 GRD 坐标精度当前 ~50m（GCP 网格极限），若客户要求米级套合 DOM，配准精度需重点验证。

> 待办：空了再探讨"自研替代 SVS 融合链路"的可行性与成本，暂不下结论。

### 8.11 前进村两种制作模式对比（面向对象 vs 面向像素）

`qianjincun_prediction_dachun_v3_dom.shp` 与 `qianjincun_cropmap.shp` 是两条不同的技术路线：

| 环节 | v3_dom.shp（面向对象） | cropmap.shp（面向像素） |
|------|----------------------|----------------------|
| 边界来源 | DOM 0.5m 掩膜 → 连通域 → SLIC 分割地块（1007 块） | 无地块边界，纯盲测 |
| 预测单元 | 每个地块（zonal mean 取均值） | 每个 10m 像素 |
| 边界精细度 | 0.5m，贴合田块 | 10m，粗糙锯齿 |
| 模型 | 旧 v3（5 区合并） | 新 23 县全量 |
| 判断机制 | 三数值联合（predicted+max_prob+conf_flag，含「不确定/非植被」档） | 硬分类 + NDVI 掩膜，无置信度 |
| 输出 | 地块标签 + 置信度 + 面积 | 分类栅格 → 平滑 → 面积过滤 → shp |

**本质**：v3_dom 是「先分割对象 → 对象取均值 → 预测对象」（object-based）；cropmap 是「逐像素预测 → 同类像素聚面」（pixel-based）。

**效果差异根因**：cropmap 在 10m 像素 + 形态学平滑 + 面积过滤下，细碎小地块被吞掉（玉米主体 2978 公顷还被 `area>1e7` 误删），所以"识别出来的东西少"；v3_dom 地块边界精细、每块一个标签，看起来更完整。

**注意**：两者模型不同（v3 vs 23 县），直接比会混入模型差异。公平比法：用同一 23 县模型分别走「地块 zonal mean」和「像素盲测」两条路，见 8.12。

### 8.12 公平对比：新 23 县模型下「面向对象 vs 像素盲测」（08-18）

用同一 23 县新模型分别走两条路，以人工标注 dltb（前进村 376 块：玉米 286、水稻 90）为真值，逐地块质心匹配对比：

| 方法 | 一致率 | 未识别率 | 水稻正确 | 玉米正确 |
|------|--------|---------|---------|---------|
| 面向对象（DOM 0.5m 分割地块 zonal mean，1007 块） | **44.4%** | 38.6% | 5.6% | 56.6% |
| 像素盲测（cropmap，124 个多边形） | **2.1%** | 96.8% | 8.9% | 0.0% |

**结论**：

1. **面向对象明显优于像素盲测**（44.4% vs 2.1%）。主因不是模型，而是 cropmap 矢量化有两处硬伤：玉米主体 2978 公顷被 `area>1e7` 误删、10m 像素 + 形态学平滑把细碎小地块吞掉，导致 96.8% 真值地块"未识别"。
2. **两者水稻都差**（5.6% vs 8.9%），这是 48SWA 6 月云量 92.72% 的底层数据限制，与制作模式无关。
3. **面向对象是前进村这类"细碎小地块"更合适的做法**；前提是先修好矢量化（去掉 1e7 误删、别对 10m 栅格做过度平滑）。

产出：`dachun_prediction/qianjincun_object_based_23counties.shp`（面向对象，新 23 县模型）。

### 8.13 面向对象预测 shp 字段说明与联合判断逻辑（08-18）

`qianjincun_object_based_23counties.shp` 字段含义：

| 字段 | 含义 |
|------|------|
| `parcel_id` | SLIC 分割地块编号 |
| `area_m2` | 地块面积（㎡） |
| `ndvi_max` | 四期 NDVI 最大值（NDVI=(B08-B04)/(B08+B04)） |
| `ndvi_flag` | 植被标志（ndvi_max≥0.30 → 植被，否则非植被） |
| `predicted` | 模型类别编码（0=水稻、1=玉米） |
| `prdct_lbl` | 模型类别名（水稻 / 玉米） |
| `max_prob` | 模型最大概率（0~1） |
| `conf_flag` | 置信度档位（≥0.70 高 / ≥0.60 中 / <0.60 低） |
| `prdct_cf` | **最终判断结果**（水稻 / 玉米 / 不确定 / 非植被 / 无覆盖） |
| `buf_pixel` | 地块内有效 10m 像素数 |

**最终结果 `prdct_cf` 由三条阈值链联合判定**：

```
buf_pixel < 1    → 无覆盖（地块未落在影像上）
ndvi_max < 0.30  → 非植被（NDVI 低于植被阈值）
max_prob < 0.60  → 不确定（模型不自信）
否则             → prdct_lbl（水稻 / 玉米）
```

三个阈值：NDVI=0.30（植被分界）、CONF_LOW=0.60（不确定分界）、CONF_HIGH=0.70（高置信分界）。

**用哪个字段看结果**：看作物 → `prdct_cf`；看可信度 → `conf_flag` + `max_prob`；看被排除原因 → `ndvi_flag` / `buf_pixel`。

### 8.14 前进村套合结果 + A01「耕种地块」讨论（08-18）

**一、套合结果（最大重叠面积匹配）**

把面向对象预测结果套合到同事地图分割 `qianjincun.shp` 上，输出 `qianjincun_crop_v3.shp`（2291 块）。质心匹配→最大重叠面积匹配后，未匹配从 372 降到 101。

| prdct_cf | 地块数 |
|----------|--------|
| 玉米 | 1848 |
| 不确定 | 243 |
| 水稻 | 99 |
| 未匹配 | 101 |

**二、相邻地块判断不一致的根因（parcel 94 案例）**

相邻 8 个地块（1923/1925/1917/1926/1928/1927/1930/1937）实为 3 块 SLIC 大地（parcel 94/4/198）。5 块"不确定"共享 parcel 94 的 max_prob=0.588（差 0.012 没过 0.60 阈值），2 块"玉米"是 parcel 4（0.807）、1 块是 parcel 198（0.930）。模型对三块都判"玉米"，仅置信度不同。

深层：parcel 94 物候像水稻（前期 NDVI 低 0.335、后期高 0.764、SWIR 低），模型判玉米但把握仅 0.588。结论："不确定"档在正确存疑，避免把疑似水稻田错判成玉米。

**三、A01「耕种地块」口径讨论**

- **口径确定**：A01 = 耕地（含休耕、撂荒），撂荒也先算耕地。
- 非耕地 = 林地 / 草地 / 建设用地 / 水域 / 裸地。
- **技术难点**：休耕/撂荒耕地与荒草地在单年遥感上难区分；采用"撂荒也算耕地"的口径后放宽，排除明显非耕地即可。
- **待办**：A01 用 NDVI 时序物候判断（区分"正在种"vs"休耕/撂荒"），判据待设计。

**四、本轮新增产出文件**

| 文件 | 说明 |
|------|------|
| `dachun_prediction/qianjincun_object_based_23counties.shp` | 面向对象预测（新 23 县模型，1007 块） |
| `dachun_prediction/qianjincun_label_crop.shp` | 人工标注（dltb，376 块，水田→水稻/旱地→玉米） |
| `地图分割数据/前进村地块边界/前进村地块边界/qianjincun_crop_v3.shp` | 套合最终版（2291 块，prdct_cf/max_prob/conf_flag/ndvi_max） |

### 8.15 A01「耕种地块」产出（08-18）

- **口径**：A01 = 耕地（含休耕、撂荒），撂荒也算耕地。
- **判据**：前进村地图分割 2291 块全部有植被（ndvi_max 最低 0.338、中位 0.673），无水域/裸地/建设用地等非耕地混入 → 全部标 A01。
- **输出**：`qianjincun_a01.gpkg`（2291 块，字段：`农业生产分类一级`="A01"、`农业生产分类二级`="耕种地块"）。
- **格式说明**：用 GPKG 而非 shp，因 Shapefile 字段名限 10 字节，中文长字段名会被截断。
- **作物识别与分类识别已分开**：作物 → `qianjincun_crop_v3.shp`；分类 → `qianjincun_a01.gpkg`。
- **村界版**：按同事村界版 id 对齐，删除村界外 111 块（2291→2180），输出 `qianjincun_a01_村界版.shp`、`qianjincun_crop_v3_村界版.shp`。

### 8.16 地物分类扩展的三点结论（08-18）

**（1）dltb 可用于训练与特征工程**
- `前进0806.gdb` 的 dltb 层有 DLMC 地类属性 + 几何边界，共 1503 要素、30 类，覆盖建筑/道路/水体/耕地/林地/草地/园地。
- 前提：① 标注空间覆盖任务区；② 类别够且样本平衡（细类只有 1~2 块则须合并成 7 大类）；③ 确定分类体系。

**（2）要补的 dltb 数据**
- 训练侧现有 22 县（4 市）：遂宁（射洪/安居/蓬溪/大英/船山）、泸州（泸县/古蔺/叙永/纳溪/龙马潭/合江/江阳）、雅安（天全/名山/汉源/芦山/荥经/雨城）、宜宾（珙县/屏山/江安）。
- 缺口一：**绵阳市（尤其江油/前进村）完整 dltb**（迁移/预测侧，训练侧无绵阳）。
- 缺口二：**训练 4 市 22 县完整地类 dltb**（扩展建筑/道路/水体分类需全部 DLMC，不只水田旱地）。

**（3）S2 影像无需矫正**
- 下载的 Sentinel-2 **L2A** 已做大气校正（地表反射率，可直接算指数）+ 正射校正（带 UTM 坐标）。
- 注意：20m 波段重采样到 10m（重采样非矫正）；云掩膜用 SCL；与 0.5m DOM 有 ~10~12m 几何偏差。
- 对比：S1 GRD 是 L1，需 DN→dB（`10×log10(DN²)`），精度 ~50m。

### 8.17 内网部署事项（待办，暂不执行）

- 目标：训练/预测流程脱离外部环境在内网运行。
- 四要素：运行环境（Python+依赖库）、坐标数据（proj.db）、数据/模型文件、操作说明。
- 待做清单：① 生成 `requirements.txt`（锁版本）；② 路径参数化（绝对路径抽成 `config.yaml`）；③ 封装 bat 一键入口；④ 写 SOP 操作手册；⑤（可选）离线 wheel 包 / conda-pack / Docker 镜像。
- 状态：**记录待办，实际需要时再做**。

### 8.18 S1+S2 融合小范围验证（08-20）

**目的**：验证加入 Sentinel-1 SAR（VV/VH）特征后，水稻/玉米二分类是否有提升。先挑 1~2 个县小范围跑通，再决定是否扩到 23 县全量。

**数据与口径**：

- S1 训练侧已下载 29 个场景（`E:\迅雷下载\0818\S1A数据`），均为 GRD IW DV，双极化 `iw-vv.tiff`/`iw-vh.tiff`。
- S1 关键属性：**无 CRS、仅带 GCP**（约 210~231 个），DN 为 uint16；重投影用 `rasterio.warp.reproject` 的 `gcps=` 参数转到 WGS84；DN→dB 用 `10×log10(DN²)`，0 视为 nodata。
- 覆盖排查结论：**5/6/7 三期覆盖较普遍，8 月多县缺失或部分覆盖**；且不能只看场景 bbox（bbox 相交但像素级可能不盖目标地块），必须做像素级有效占比确认。
- 踩坑：天全县 8 月场景 `9A3A` bbox 看似覆盖，实际像素级只盖玉米、水稻覆盖 0/224，导致"剔缺期地块"后只剩玉米单类、跑出 Acc=1.000 假象；已改用像素级覆盖校验规避。

**验证县选择**：名山区（48RUU）+ 荥经县（48RTT）。两县 5/6/7 三期 S1 覆盖完整（水稻+玉米都有）；8 月 S1 不全，故只验证三期。

**特征构建**：

| 侧 | 特征维度 | 说明 |
|----|---------|------|
| S2 | 154 | 3 期 × 10 波段 → `compute_feature_matrix`（原始波段+指数+时序统计+物候+差值） |
| S1 | 19 | 3 期 × (VV/VH) + 3 期 VV−VH + 2 极化 × 5 时序统计（min/max/mean/std/range） |

- 融合特征选择 Top40 中 **SAR 占 12 个**，Top10 含 `SAR_P3_VV`、`SAR_P3_VVminusVH`（即 7 月 SAR 信号）。

**验证结果**（样本：两县共 1598 块，水稻 1159 / 玉米 439，剔除任一期次 NaN 后完整 1586 块；模型 LightGBM 二分类）：

| 验证方式 | 指标 | S2-only | S1+S2 融合 |
|---------|------|---------|-----------|
| LOGO 留一县（空间泛化） | 平均 Acc / F1 | **0.4757 / 0.3664** | 0.4782 / 0.3705 |
| ├ 名山区留出 | Acc | 0.2599 | 0.2599 |
| └ 荥经县留出 | Acc | 0.6915 | 0.6965 |
| 重复分层 K 折（同样本内，5折×3次） | Acc / F1 | **0.8775 / 0.8750** | 0.8762 / 0.8734 |
| ├ 水稻 F1 | — | 0.9173 | 0.9167 |
| └ 玉米 F1 | — | 0.7627 | 0.7586 |

**结论**：

1. **同样本内（分层 K 折）加入 S1 无增益**：Acc 0.8775→0.8762、玉米 F1 0.7627→0.7586，甚至略有下降。说明当前三期 VV/VH SAR 信号对水稻/玉米区分贡献有限（S2 多光谱的水分/物候信号已基本覆盖）。
2. **跨县 LOGO 也基本持平**（0.4757→0.4782，仅荥经县 +0.005），S1 未补强空间泛化。
3. **名山区留出 Acc 仅 0.2599（远低于随机）**：这是名山区与荥经县特征分布差异大/空间异质导致的，与是否加 S1 无关；分层 K 折 0.877 说明模型本身能学好，但跨县留出暴跌，提示这两县做 LOGO 的空间泛化基准本身就偏弱，小范围验证的代表性有限。

**决策建议**：现阶段 **S1 融合收益不明显，建议维持 S2-only**。若要进一步挖掘 S1，需①补 8 月完整覆盖场景（凑满四期对齐）；②换更强 SAR 特征（纹理 / H-A-Alpha 极化分解 / 双极化比值 VV/VH 原始比而非 dB 差）。

**产出文件**：

| 文件 | 说明 |
|------|------|
| `train_s1s2_validation.py` | S1+S2 融合验证脚本（选县→S2/S1 提取→LOGO+分层 K 折对比→出 report/CSV） |
| `s1s2_validation/report.json` | 指标对比 JSON（含 LOGO 与分层 K 折明细） |
| `s1s2_validation/features_s1s2.csv` | 中间特征（S2 154 + SAR 19 + ID/县/类别） |

### 8.19 S1+S2 融合验证扩展（5 县，08-20）

在 8.18 名山+荥经 2 县基础上，扩到更多县复验，并排查"能否加 8 月期"。

**一、8 月期不可用的根因（不是没数据，是选景选了升轨）**

对候选县做像素级覆盖检查（10m 网格 + bincount 统计）：**已下载的 8 月 S1 场景对水稻零覆盖**（天全 8月 0/96、荥经 0/128、芦山 0/57、雨城 0/66、名山无覆盖），8 月场景（`9A3A`/`7A04` 等）主要落在川南玉米带。

排查根因（`_gen_s1_links.py` + STAC 复查）：

1. 生成下载清单的脚本「**优先 ascending 升轨**」+「**S1 场景 bbox 与瓦片 bbox 相交即判覆盖**」。S1 IW 条带 bbox 很大，8 月升轨（傍晚 11:xx UTC）场景 bbox 擦到雅安瓦片就被误判为"覆盖"，于是选了升轨。
2. 而这些升轨场景像素级只覆盖川南玉米带，盖不到雅安水稻。
3. 真正覆盖雅安水稻的是 **descending 降轨（清晨 23:xx UTC）** 轨道——5/6 月的 `45E8`/`8E10` 就是这条。8 月该轨道其实有场景（07-31 / 08-05 / 08-06 / 08-11 / 08-18），但被"优先升轨"顶掉，一个都没下。

结论：**8 月不是没数据、也不是文件下错，而是"选景选了升轨、漏了降轨"。补下 8 月 descending 场景即可凑齐四期。**

**二、可扩县范围**

5/6/7 三期 S1 覆盖完整（≥94%）且水稻+玉米都有的县 = 雅安 5 县（5/6 月降轨 `45E8`/`8E10` + 7 月升轨 `9DD8` 覆盖）。屏山/汉源全玉米、其余县 6 月无覆盖，不纳入。

| 县 | 水稻 | 玉米 | 5/6/7 覆盖 |
|----|------|------|-----------|
| 名山区 | 1031 | 365 | 100% |
| 天全县 | 96 | 1469 | 94.5% |
| 芦山县 | 57 | 184 | 99.6% |
| 荥经县 | 128 | 74 | 100% |
| 雨城区 | 66 | 73 | 99.3% |

合并 3543 块（水稻 1378 / 玉米 2165），剔除任一期次 NaN 后完整 3444 块。

**三、结果**

| 验证方式 | 指标 | S2-only | S1+S2 |
|---------|------|---------|-------|
| 分层 K 折（同样本内，5折×3次） | Acc / F1 | 0.8930 / 0.8927 | **0.8970 / 0.8968** |
| ├ 水稻 F1 | — | 0.8639 | 0.8691 |
| └ 玉米 F1 | — | 0.9118 | 0.9151 |
| LOGO 留一县 | Acc / F1 | **0.5483 / 0.5451** | 0.5480 / 0.5374 |

LOGO 分县：天全县 0.5199→0.5713（唯一明显提升），名山 0.3870→0.3827、荥经 0.4257→0.4059、雨城 0.7464→0.7174 均微降。

**四、结论**

1. 5 县下 SAR 特征在 Top40 中占 **6 个**（Top10 无 SAR），比 2 县时（12 个、Top10 有 2 个）更弱。
2. 同样本内（分层 K 折）S1 带来**一致但微弱**的正增益（各指标 +0.003~0.005），与 2 县时"无增益甚至略降"不同——样本更丰富后 SAR 的微弱区分信号才显现。
3. 跨县 LOGO 仍**无提升**（Acc 持平、F1 略降），S1 增益不转化为空间泛化。
4. **总体判断不变**：S1 融合收益过小（<0.5pp）且不稳定，建议维持 S2-only；除非换更强 SAR 特征（纹理/极化分解）或拿到覆盖水稻的 8 月场景。

**脚本改动**：`extract_s1` 改为**按县分组建网格**（避免多县合并网格过大拖慢 reproject）；`COUNTIES` 扩为 5 县。`s1s2_validation/report.json`、`features_s1s2.csv` 已更新为 5 县版本。

---

## 九、江油迁移三版验证 + 作物识别要求 vs 现状 vs 缺口 + 小春数据盘点（08-21）

### 9.1 6 月影像补齐后的江油迁移三版对比

**背景**：8.18 已定位前进村水稻失败的根因是「48SWA 6 月云量 92.72% 盖死 + 48SVA 06/07/08 只覆盖半幅」。本次把 6 月关键时相补齐，量化「数据质量」到底能救回多少精度。

**补图候选**（STAC 检索选定，均为覆盖江油的景）：

| 瓦片 | 场景 | 日期 | 云量 | 用途 |
|------|------|------|------|------|
| 48SVA | `S2C_48SVA_20250604_0_L2A` | 06-04 | 34.75% | 补 6 月（原 06 文件夹为空） |
| 48SWA | `S2C_48SWA_20250525_0_L2A` | 05-25 | 35.51% | 替代 6 月（替代 1） |
| 48SWA | `S2B_48SWA_20250709_0_L2A` | 07-09 | 49.72% | 替代 6 月（替代 2） |

**三版迁移结果**（江油 5 村真值，水稻/玉米约 1.1 万块）：

| 版本 | 48SVA 06 | 48SWA 06 | Acc | F1 | 水稻召回 | 玉米召回 |
|------|----------|----------|-----|-----|---------|---------|
| 基线 | 空 | 云景 92.72% | 0.1772 | 0.2277 | — | — |
| **A** | 06-04 | 05-25 | **0.6001** | **0.7082** | 355/592 | 6416/10691 |
| B | 06-04 | 07-09 | 0.5960 | 0.7049 | 355/592 | 6370/10691 |

**瓦片覆盖分配**：48SVA 覆盖 16,624 块（88%），48SWA 覆盖 2,174 块（12%）。

**结论**：

1. 补齐 6 月影像后，江油迁移精度从 17.72% 跃升至 60.01%，**证明此前精度崩塌主要是「6 月影像缺失/被云盖」的数据质量主导**，不是模型失效。
2. 48SVA 覆盖 88% 地块，是精度主要决定因素；48SWA 两替代（05-25 vs 07-09）差异仅 0.4pp，说明替代景选择对整体影响很小。
3. 60% 之后仍差 95% 目标约 35pp，剩余差距主要来自 **48R（训练）→48S（推理）跨带域偏移**，需江油本地微调/重训，不是再补影像能解决的。

### 9.2 《作物识别要求.md》要求 vs 现状 vs 缺口

依据 `作物识别要求.md`（测绘仅中标区域、田埂精度 0.5m、最小上图一分地、整体准确率 ≥95%），逐项对照：

| 维度 | 要求 | 现状 | 缺口 |
|------|------|------|------|
| **作物类别** | 水稻/玉米/小麦/油菜/蔬菜/柑橘/蜜柚/中草药 | 仅水稻/玉米二分类可用；小麦/油菜有初步模型但未达标 | 蔬菜/柑橘/蜜柚/中草药 4 类无模型、无标注 |
| **水稻/玉米（大春）** | 准确识别（≥95%） | 绵阳迁移整体 Acc 83.31%；江油 67.7%；前进村水稻识别失败 | 江油/前进村差；整体距 95% 差 ~12pp；需本地微调 |
| **小麦/油菜（小春）** | 准确识别（≥95%） | 遂宁 3 区 Acc 81.57%，油菜 Recall 仅 56.3% | 精度不达标；绵阳 5 县无小春标注、无影像 |
| **蔬菜** | 统一归「蔬菜」 | 无模型 | 未覆盖 |
| **柑橘/蜜柚/中草药** | 多年生水果识别至品种；林下药材可协商 | 无模型 | 未覆盖（多年生/林下，需完全不同的数据与方法） |
| **地域（绵阳）** | 梓潼/盐亭/三台/平武/江油 5 县 | 仅水稻/玉米跑过绵阳迁移；江油偏低，前进村未达标 | 4 县小春及其他作物全缺 |
| **精度口径** | 田埂 0.5m、最小上图一分地（≈66.7㎡） | Sentinel-2 为 10m（单像元 100㎡） | 哨兵无法达到 0.5m 田埂/一分地，须高分影像（0.5m DOM）做边界 + 哨兵做作物分类 |

**关键判断**：当前成果只覆盖「水稻/玉米」这一个子任务，且未达 95%。整个《要求》清单里小麦/油菜/蔬菜/柑橘/蜜柚/中草药基本是从零开始。**0.5m 田埂与一分地的最小上图精度，10m 哨兵先天做不到，必须走「高分影像做边界 + 哨兵做类别」的路线**（前进村已验证此路线可行）。

### 9.3 小麦/油菜：数据够不够、影像要不要重下

**（1）训练数据：不够达标，但非零基础**

| 项 | 现状 |
|----|------|
| 已训练 | 遂宁 3 区（安居/大英/船山）小麦+油菜 18,071 块（小麦 11,105 / 油菜 6,966），小春模型 Acc≈81.57%，油菜 Recall 仅 56.3% |
| 存量未用 | `2024小春\广安市` 5 区（前锋/华蓥/广安/武胜/邻水）shapefile 已下载，**尚未并入训练**（可扩展样本） |
| 绵阳本地标注 | 无（`绵阳标注_水稻玉米.gpkg` 只有大春水稻/玉米） |

- 结论：**当前数据量级（1.8 万块）可以训练一个初步模型，但离 95% 差得远**，尤其油菜 Recall 只有 56.3%（样本偏少 + 类不平衡）。要达标需：① 并入广安 5 区存量样本；② 针对油菜做类不平衡/样本扩增；③ 争取绵阳本地小春标注做迁移验证。

**（2）影像：必须重新下**

- 小麦/油菜是**小春作物**（秋播，次年 5 月收），物候窗口与现用大春 5/6/7/8 月完全不同：
  - 12 月越冬 → 1 月休眠 → 3 月返青/油菜开花 → 5 月收获
- 现用的大春影像（5/6/7/8 月）**完全不适用**，需要 **12 / 1 / 3 / 5 月**四期。
- 已有：遂宁 48RWU 四期齐全（小春实验已用）；江油前进村部分已有。
- 缺口：**绵阳其余县（梓潼/盐亭/三台/平武）小春影像未下**，需按 12/1/3/5 月窗口重新 STAC 检索并下载。

### 9.4 江油本地微调/重训「留一村」验证（08-21）

**目的**：验证「用江油本地标注微调/重训」能否救回零样本 60% 的精度。做法：江油 5 村水稻/玉米真值（11,283 块，水稻 592 / 玉米 10,691），每村留出当测试、其余 4 村训练，对比四种方案。

| 方案 | Acc | F1 | **balAcc** | 水稻召回 | 玉米召回 |
|------|-----|-----|-----------|---------|---------|
| S0 零样本（23县模型） | 0.6001 | 0.7082 | 0.5999 | 0.5997 | 0.6001 |
| S1 留一村重训 | 0.9513 | 0.9381 | 0.5857 | 0.1774 | 0.9941 |
| S2 留一村微调（init_model） | 0.9491 | 0.9386 | 0.6022 | 0.2145 | 0.9898 |
| S3 留一村重训+类别平衡 | 0.9019 | 0.9129 | **0.6570** | 0.3834 | 0.9306 |

> `balAcc`（平衡准确率）=（水稻召回 + 玉米召回）/2，是唯一不受「玉米占 94.8%」类别不平衡影响的公平指标。

**分村水稻样本极不均衡**：大岳村 286 / 龙宫村 104 / 沉水村 118 / 印坪村 69 / **马阁寺村仅 15**。

**结论**：

1. **Acc/F1 的"95%"是类别不平衡的假象**：水稻只占 5.2%，模型只要"全判玉米"就能拿 94.8% 的 Acc。S1/S2 的 Acc 0.95 背后是玉米召回 99%、水稻召回跌到 18~21%——模型退化成了"玉米检测器"。
2. **用 balAcc 看，四方案都在 60~66%，本地重训/微调没有实质提升**：S1 甚至比零样本还低（0.586 vs 0.600）。本地重训只是把"玉米误判成水稻"翻转成"水稻误判成玉米"，没解决根本问题。
3. **加类别平衡（S3）是唯一有正面效果的**：balAcc 0.60→0.66，水稻召回恢复到 38%，但离 95% 仍差很远。
4. **根因不在"缺本地标注"，而在"水稻/玉米在江油的特征本身区分不开"**——水稻识别依赖 6 月淹水/返青信号（LSWI/NDWI），而江油 6 月影像质量差（48SVA 06-04 云量 34.75%、48SWA 用 05-25 替代而非真 6 月），这个信号被破坏。

**判断**：**本地微调/重训这条路在当前影像与样本下不成立**（对水稻识别无实质帮助、甚至有害）。要突破 60% 的正确方向是：① 拿到江油真正低云的 **6 月淹水期影像**（而非本地标注）；② 若仍要本地化，必须配合类别平衡 + 保留 23 县水稻先验。产出脚本 `validate_jiangyou_leave_one_village.py`，结果 `jiangyou_lov/lov_summary.json`、`lov_per_village.csv`。

### 9.5 江油 6 月低云影像「三年对比」检索坐实（08-21）

对江油 bbox 做 2024/2025/2026 三年 5/25~7/05 全量检索（footprint 级覆盖判定），结论：

| 年份 | 6 月前后最优云量 | 是否有低云(≤20%) |
|------|----------------|-----------------|
| 2024 | 33.94%（48SWA 05-30） | 无 |
| 2025（当前在用） | 34.75%（48SVA 06-04） | 无 |
| **2026** | **18.03%（48SWA 05-30）/ 18.85%（48SVA 05-30）** | **有** |

- 2025 年覆盖江油且云量 ≤60% 的只有两景（48SVA 06-04 34.75%、48SWA 05-25 35.51%），其余 73%~100% 云 → **江油 2025 年 6 月低云影像客观不存在**，已下载的即最优解。
- 2024 年同样无低云（最优 33.94%）。
- **2026 年有低云**：05-30（18%）、06-04（27.5%）、06-09（28.5%）、06-19（29.8%）等多个可选项。

**关键约束（影像年份 = 标签年份）**：水稻/玉米是轮作作物，同一块地不同年种的可能不同。影像必须与 BDLB 标的（标签）同一年，否则张冠李戴。因此：
- 若江油 BDLB 标签是 **2025 年** → 只能用 2025 影像，6 月无低云是硬约束（卡 34.75%）。
- 若江油 BDLB 标签是 **2026 年** → 应改用 2026 影像，6 月有低云（18%），有望大幅改善水稻识别。

> **待确认（重要）**：江油 BDLB 标的（水稻/玉米标签）到底标注的是哪一年？这决定该用 2025 还是 2026 影像，是当前最优先要跟用户/数据方核对的问题。

### 9.6 小麦样本量盘点（08-21）

实测 `2024小春` shapefile（字段 `ZWMC` 作物名）：

| 来源 | 小麦块数 | 状态 |
|------|---------|------|
| 遂宁 3 区（安居 8692 / 大英 2715 / 船山 523） | ~11,930 | 已训练（报告口径 11,105） |
| 广安 5 区（广安 1554 / 武胜 799 / 邻水 595 / 华蓥 284 / 前锋 168） | 3,400 | 已下载，未并入训练 |
| 绵阳本地 | 0 | 无小春标注 |

**结论**：小麦合计约 1.4~1.5 万块，**样本量够训初步模型，但不足以直接交付 95%**——现小春模型（仅遂宁 3 区）Acc≈81.57%，离目标差 13pp+；真实缺口是「跨市迁移未验证 + 绵阳无标注 + 广安/绵阳小春影像未下」。

### 9.7 江油 6 村「局部」云量诊断 + 破局思路（08-21）

标签年份确认 = **2025 年**，故影像只能用 2025。对江油 6 村地块做**像素级**云检测（B02>2500 亮像元占比，`_diag_jiangyou_cloud.py`），结果：

| 瓦片（覆盖） | 5月 | 6月 | 7月 | 8月 |
|------|-----|-----|-----|-----|
| 48SVA（88% 地块） | 0% | 38%（水稻 24% / 玉米 39%） | 0% | 0% |
| 48SWA（12% 地块） | 0% | 54%（05-25 替代景） | 19% | 2.5% |

- **5/7/8 月江油几乎完全无云，仅 6 月有云**；6 月水稻地块实际只被云遮 24%（非全废）。
- 推翻「6 月全废、无解」假设：问题定位为「**仅 6 月部分云污染了特征**」。
- 破局思路（比"换 7 月推断 6 月"更优）：
  1. **6 月云掩膜**：剔除 B02>2500 云像元后再算 zonal mean（水稻仅损失 24% 像元）；
  2. **5 月淹水信号强化**：淹水信号 5 月移栽期最强且江油 5 月无云，检查 05-20 是否偏早可微调；
  3. 7 月推断不出 6 月的"淹水"物理信号（田面水 7 月已干），不能替代，只能作时序辅助。

### 9.8 6 月云掩膜验证（`validate_jiangyou_cloudmask.py`，08-21）

给 `extract_features` 加 `cloud_mask_threshold` 参数，用 B02>2500 亮像元做云掩膜后重算 zonal mean，江油零样本迁移对比：

| 指标 | 基线（无掩膜） | 云掩膜(B02>2500) | 变化 |
|------|--------------|-----------------|------|
| Acc | 0.6001 | 0.3901 | -0.21 |
| F1 | 0.7082 | 0.5119 | -0.20 |
| balAcc | 0.5999 | 0.5744 | -0.03 |
| **水稻召回** | 0.5997 | **0.7804** | **+0.18** |
| 玉米召回 | 0.6001 | 0.3684 | -0.23 |

**结论**：
1. **水稻召回 60%→78%（+18pp）直接坐实「6 月云污染是水稻识别差的关键因素」**——剔除云像元后水稻淹水信号恢复。
2. 但玉米召回 60%→37% 崩掉，balAcc 基本持平（甚至略降）。原因是 **B02>2500 阈值太粗**：把玉米旱地/裸土的亮像元也误判成"云"剔除了，导致玉米特征失真、被误判成水稻。
3. 下一步（明确）：**改用 Sentinel-2 自带 SCL 波段做精确云掩膜**（SCL 8/9=云、3=云影，能区分云 vs 裸土 vs 植被），替代 B02 亮像元近似，验证能否"水稻召回升、玉米召回不崩"。

### 9.9 SCL 精确掩膜三版对比（`validate_jiangyou_scl.py`，08-21）

下载 6 月 SCL.tif（48SVA 06-04 / 48SWA 05-25），`extract_features` 加 `cloud_mask_scl` 参数（SCL 3/8/9/10=云影/云），三版对比：

| 指标 | ① 基线 | ② B02>2500 | ③ SCL 精确 |
|------|-------|-----------|-----------|
| Acc | 0.6001 | 0.3901 | 0.3713 |
| F1 | 0.7082 | 0.5119 | 0.4894 |
| balAcc | 0.5999 | 0.5744 | 0.5964 |
| **水稻召回** | 0.5997 | 0.7804 | **0.8480** |
| **玉米召回** | 0.6001 | 0.3684 | **0.3449** |

**结论**：
1. **SCL 精确掩膜把水稻召回从 60% 拉到 85%（+25pp）**，证明「6 月云污染」确被精确解决，方向完全正确。
2. 但玉米召回 60%→34% 崩掉，且 SCL（34%）比 B02（37%）更差——**说明玉米崩不是"误删像元"，而是掩膜后模型整体偏向水稻**（玉米 65% 被误判成水稻）。
3. **三版 balAcc 全部卡在 60%**：掩膜、微调、重训、类别平衡（9.4）都只能小幅扰动、无法突破。**江油水稻/玉米判别上限 ≈ 60%，根因是 48R→48S 跨带域偏移，不是云/样本量/掩膜方法。**
4. 真正方向：**SCL 掩膜 + 类别平衡重训**（掩膜救水稻召回、类别平衡救玉米召回）是唯一可能再往上走的路，但需重提 23 县训练特征 + 重训，成本较高；否则应接受「零样本 balAcc 60% 是当前影像/模型组合的域偏移上限」。

---

## 十、转向小春（小麦/油菜）路线（08-21 决策）

大春水稻/玉米经 5 种方法（零样本/重训/微调/类别平衡/云掩膜）交叉验证，balAcc 始终卡在 60%，判定为 **48R→48S 跨带域偏移上限**。用户决策走 **B：转小麦/油菜**。

### 10.1 小春影像获取：改走 GEE 云上处理（08-21 决策）

用户提出三问（影像 GEE 是否有 / 能否不下载直接在 GEE 跑 / 以后如何自主选景），结论：
- Sentinel-2 L2A 在 GEE 有完整镜像：`COPERNICUS/S2_SR_HARMONIZED`（含 10 波段 + SCL + QA60），与 Element84 COG 同源；
- **可在 GEE 直接提取特征/训练/推理，无需下载影像**（唯一导出的是特征 CSV，几 MB）；
- **GEE 自动按「区域 bbox + 日期窗口 + 云量」选景**，彻底消除"以后怎么确定下哪个瓦片"的问题。

本地清单仍保留作备选（`_gen_xiaochun_links.py` 已生成 80 景：20 瓦片×4 时相，73 景 ≤20% 低云）：
`小春影像下载清单.txt/.csv`。

**当前阻塞**：用户无 GEE 账号，需先注册（需 Google 账号 + 代理；注册入口 signup.earthengine.google.com，选 Non-commercial，通常几分钟~2 天审批）。

### 10.2 待办（走 B 路线，GEE 方案）

1. 用户注册 GEE 账号（需 Google 账号 + 代理；注册后告诉我）；
2. 我写 GEE 特征提取脚本（`COPERNICUS/S2_SR_HARMONIZED`，12/1/3/5 窗口选低云 + reduceRegions）+ 训练/验证脚本；
3. 向数据方确认**绵阳本地小春标注**（江油前进村已知小麦906/油菜597，梓潼/盐亭/三台/平武 4 县待确认）；
4. 合并广安 3,400 块小麦进训练，做类别平衡（油菜召回仅 56% 是短板）；
5. 迁移验证（遂宁/广安训练 → 绵阳），关注跨市域偏移（参考大春教训）。

---

## 十一、GEE 小春特征提取 + 训练（08-21 完成）

### 11.1 GEE 环境打通

- 认证成功，项目 ID `neat-shell-506206-q0`（名称 "My Project 27848" 只是显示名，真实 ID 是 `neat-shell-506206-q0`），已 `earthengine set_project` 固化。
- Python API `earthengine-api 1.7.40` 装好，`ee.Initialize()` 可直接调用。

### 11.2 GEE 特征提取（免下载）

`gee_extract_features.py`：读本地 shp → S2 L2A 按 4 时相窗口（12/1/3/5，跨年 2024-12~2025-05）做 **SCL 云掩膜 + median 合成** → `reduceRegions` 提取 4期×10波段 zonal mean。

关键技术点（踩坑记录）：
- 地块 shp 几何顶点极多，直接传 GEE 会超 10MB payload；**简化几何 tolerance=0.0001（约10m）+ 大县分批（>5000块按3000/批，每批用子集范围独立合成）** 解决。
- `getInfo` 逐批慢的根因是"每批重算整个县的 median 合成"，改成"每批用子集 roi 独立合成"后大幅提速。

产出：
- `gee_遂宁3区_特征.csv`：19,805 块（小麦 11,930 / 油菜 7,875），43 列（fid/ZWMC/QXMC/40波段）。
- `gee_广安5区_特征.csv`：3,426 块（前锋194/华蓥284/广安1554/武胜799/邻水595）。

### 11.3 小春训练结果（遂宁+广安合并，LightGBM，类别平衡）

`train_xiaochun_gee.py`：波段映射(B2→B02)+日期映射 → `compute_feature_matrix` 172 维 → LightGBM 二分类。

| 指标 | 之前（遂宁3区） | 现在（+广安+类别平衡） |
|------|----------------|----------------------|
| 整体 Acc（分层5折） | 81.57% | **81.4%** |
| **油菜召回** | **56.3%** | **85.4%（+29pp）** |
| 小麦召回 | — | 79.4% |

**留一县验证**（8 县）：平均 Acc 86.6%；安居区（最大县，13,317 块）留出仅 **70.9%**，前锋/华蓥/邻水/广安（广安小县）98~100%。

### 11.4 关键结论

1. **GEE 云端路线闭环**：免下载影像，云端提取特征 → 本地训练，一套流程可复用。
2. **油菜短板已补**：56%→85%，类别平衡 + 广安数据起效。
3. **3 月（返青/油菜开花）是最关键时相**：Top 特征全是 EVI/NDWI/LSWI_2025-03，印证油菜靠 3 月花海、小麦靠返青区分。
4. **整体仍离 95% 差 ~14pp**，瓶颈清晰：安居区是硬骨头（70.9%），跨县迁移回落风险与大春安居区规律一致。

### 11.5 待办

1. 确认绵阳本地小春标注（梓潼/盐亭/三台/平武），做遂宁/广安 → 绵阳的跨市迁移验证；
2. 针对安居区做专项（或增加安居区内部差异样本）；
3. 若需冲 95%，考虑 S1 SAR 融合（此前 v4_sar 曾把油菜召回提到 84%，但整体 Acc 未升）。

### 11.6 小春标注暂无 + 其他作物可做方向（08-21）

**小春标注**：数据方暂无法提供绵阳本地小春标注（江油前进村以外 4 县均无），小春跨市迁移验证**暂缓**；遂宁/广安训练成果已固化（油菜召回 85%、整体 81.4%）。

**其他作物现状盘点**（依据《作物识别要求.md》清单）：

| 作物 | 现状 | 可做性 |
|------|------|--------|
| 蜜柚（梓潼） | 有「果园 vs 非果园」二分类雏形（叙永训练，`果树识别_step1_coldstart`），**但无品种级模型**；果园靠 DEM 主导、跨区迁移有问题 | 中（有雏形，缺绵阳蜜柚标注 + 品种级） |
| 柑橘（平武/江油） | 同上，叙永有李子+柑橘标注，绵阳无柑橘标注 | 中 |
| 蔬菜（平武/江油） | 无模型，大春标注库仅 38 块 | 低（需大量标注） |
| 中草药（平武） | 无模型，林下无法光学识别 | 低（林下可协商） |

**最该优先做的（硬性门槛）**：`作物识别要求.md` 要求田埂 0.5m + 最小上图一分地（≈66.7㎡），10m 哨兵先天做不到，**必须走「高分影像做边界 + 哨兵做类别」**（前进村已验证可行）。这是所有作物识别的地基。

### 11.7 果园模型摸排：输入输出 + 迁移瓶颈 + 品种级可行性（08-21）

用户要求摸清叙永果园模型，判断能否接续做品种级；若不能，至少先解决「果园 vs 林地」区分。

**（1）叙永果园模型（输入→输出）** — [run_transfer_learning.py](果树识别_step1_coldstart/scripts/run_transfer_learning.py)

- 输入 ≈70 维：4 季 S2 光谱（10 波段×4）+ 4 季植被指数（NDVI/NDRE/NDMI/EVI）+ 物候差值 + 盛夏纹理（3×3/7×7）+ SAR 后向散射（VV/VH×2 季）+ DEM（海拔/坡度）。
- 正样本：叙永 `result.shp` 果树地块（**李子 190 + 柑橘 86 = 276 块**），栅格化成「果园」。
- 负样本：WorldCover 非树类别 `[40,50,60,70,80,90]`（农田/建筑/裸地/水/湿地）。
- 输出：遂宁全境果园二值/概率栅格 + 斑块矢量。

**（2）迁移瓶颈（按严重程度）**

1. **致命：负样本没有「天然林地」**。`neg_cats` 不含 Tree cover(10)，模型从没见过天然林，而果园与天然林同为常绿木本、光谱极像 → 迁移到遂宁把大片林地误判成「果园」。证据：`transfer_report.json` 遂宁「果园」达 **350,625 ha**，远超真实值，几乎必是林地误分。
2. 跨带域偏移：叙永 48RWR → 遂宁 48RWU（与大春水稻玉米同类问题）。
3. 无真值验证：迁移只有面积统计，无精度/召回/混淆矩阵，离谱数字一直没被发现。

**（3）品种级可行性 — 现阶段不建议直接做**

`大春训练标注库.gpkg`（G 盘汇交合并）果树共 **527 块、10 品种**，但极度不均衡：

| 品种 | 块数 | 品种 | 块数 |
|---|---|---|---|
| 李子 | 212 | 荔枝 | 10 |
| 柑橘 | 104 | 橘子 | 9 |
| 桂圆 | 72 | 枇杷 | 8 |
| 梨 | 48 | 柑 | 3 |
| 桃子 | 36 | 柠檬 | 25 |

仅李子/柑橘/桂圆/梨够样本，其余个位数；且分散在 5 县（叙永/泸县/龙马潭/汉源/蓬溪）、不同瓦片，跨县迁移叠加品种细分难度翻倍。**结论：品种级先不做，连「果园 vs 林地」都没分对，品种级是空中楼阁。**

**（4）果园 vs 林地 — 当前最该补、方案明确**

1. 负样本补林地：WorldCover `Tree cover(10)` 加进负样本（正样本栅格化覆盖果树后，剩余 Tree cover 即天然林）。
2. 加「常绿 vs 落叶」物候特征（果园冬季仍管理、NDVI 时序更稳，天然林波动大）。
3. 必须加真值验证：用安居区现成 DOM 目视圈「天然林」真值块，算召回/混淆矩阵，不再只看面积。

**（5）下一步待办（下班前定格，下次从这里接）**

- 用户尚未确认是否动手改 `run_transfer_learning.py`（补林地负样本 + 常绿/落叶特征 + 真值验证），等用户回复方向后再执行。
- 关联线索：`run_suining_binary.py` 第 482-483 行已有「Tree cover 作天然林负样本」的正确思路（但该脚本 acc=1.0/非果园=0ha 有数据泄漏问题，仅思路可参考）。

### 11.8 果园 vs 林地实验（补林地负样本，08-21）

**实验**：新写 `retrain_orchard_forest.py`（复用已缓存特征，不重下哨兵影像），把 WorldCover `Tree cover(10)` 加入叙永模型的负样本，重建标签 → 重训 LightGBM → 遂宁推理。

**结果**：

| 指标 | 旧模型（负样本无林地） | 新模型（补 Tree cover） |
|------|----------------------|----------------------|
| 遂宁"果园"面积 | **350,625 ha** | **197 ha** |
| Tree cover 被判成果园比例 | 大量误报 | 仅 0.01% |

**结论：过度校正**。旧模型把"树"全当果园（林地误报），新模型把"树"全当林地（果园漏报）。两个都是极端。

**根因诊断**（关键）：
1. WorldCover 的 `Tree cover(10)` **不是干净林地标签**——它混合了天然林 + 树冠覆盖高的果园，用它做负样本等于告诉模型"树=林地"，把果园也压掉。
2. 新模型留下的 197 ha"果园"，其 **winter_ndvi 中位数仅 0.21**（全图 0.43）、summer_ndvi 仅 0.27——说明模型实际只学会了「冬季落叶/稀疏植被 = 果园」，本质是**「落叶 vs 常绿」**，而非「果园 vs 林地」。
3. **常绿柑橘园 vs 常绿天然林**（冬季 NDVI 都高）在 10m 哨兵光学上**本质难分**，这是物理上限。

**下一步方向（待用户定）**：
- A. 用安居区 DOM 高分影像目视圈「真林地 + 真果园」真值，做有监督训练（最可靠，需人机协作）；
- B. 接受哨兵 10m 光学对「常绿果园 vs 常绿林」的天花板，回到「高分影像做边界 + 哨兵做类别」主路线；
- C. 分品种建模：落叶果园（李子）靠冬季物候可分，常绿果园（柑橘）需高分/纹理/SAR。

**用户决策（08-21）**：暂停果园，先做别的。果园现状定格——叙永模型负样本漏林地、补林地又过度校正，根因是「常绿柑橘 vs 常绿天然林」在哨兵 10m 光学上到物理天花板，需高分 DOM 才能进一步突破；相关脚本已留存（`retrain_orchard_forest.py` / `validate_orchard_forest.py`）。

### 11.9 小春小麦/油菜 95% 攻坚（08-24）

**目标**：从遂宁+广安合并模型整体 Acc 81.4%（油菜召回 85.4%、小麦召回 79.4%）继续逼近 ≥95%。

**诊断链条**（新脚本 `diag_xiaochun_ndvi.py` / `diag_anju_misclass.py` / `diag_anju_label_noise.py`）：

1. **NDVI 时序分析**：小麦/油菜都是越冬绿色作物，光谱高度重叠；单特征最优阈值只能分到 **67%**（NDVI_12月 0.674）。唯一明显可分时相是 3 月（返青/油菜开花，小麦 NDVI 0.645 vs 油菜 0.548）。

2. **波段归一化修复**（`exp_scale_fix.py`）：GEE 波段值是 ×10000 DN，`compute_feature_matrix` 里 EVI/SAVI 的 `+1.0/+0.5` 常数按 0-1 反射率设计导致失真。除以 10000 修复后 **Acc 81.41%→81.35%，无效**，排除特征工程问题（树模型对单调缩放不敏感）。

3. **安居区标签噪声（决定性发现）**：两个独立实验（安居区内 5 折 + 外县模型预测）都显示，安居区约 1896~2987 个"小麦"标签的物候特征与油菜几乎完全重合——3 月 NDVI 仅 0.531（正确小麦 0.645、油菜 0.548），EVI 时序 3~5 月与油菜一致。**安居区约 21.8% 的"小麦"标签实为油菜（标签噪声）**，这是整体卡 81.4% 的根因。

4. **标签清洗验证**（`exp_label_cleaning.py`，无泄漏）：用非安居区模型预测安居区概率，翻转高置信度可疑标签后重训。

| 处理 | 翻转数 | Acc | 小麦 recall | 油菜 recall | 油菜 precision |
|------|-------|-----|-----------|-----------|--------------|
| 基线 | 0 | 81.4% | 79.4% | 85.4% | 68.1% |
| 阈值0.7清洗 | 2292 | **89.3%** | 88.7% | 90.1% | 84.3% |
| 阈值0.8清洗 | 1556 | 87.2% | 86.8% | 87.8% | 80.8% |

5. **加密时相验证**（`gee_extract_features_v2.py` + `diag_chuanshan_compare.py`）：把 4 期（12/1/3/5月）加到 6 期（补 2月返青初/4月结荚抽穗），船山区验证 **Acc 83.15%→83.82%，仅 +0.7pp**，小麦 recall 61.8%→64.1%。证明时相已饱和，非瓶颈。

**最终结论**：

- **95% 在 10m 哨兵光学上物理不可达，天花板约 89%**。
- 之前卡 81.4% 的根因是安居区约 8% 标签噪声（非模型问题）；清洗后两类 recall 88~90%、precision 84~93% 是当前数据真实可分性。
- 剩余 ~6pp 误差来自小麦/油菜光谱本质重叠（越冬密植绿作物），加密时相/调参/SAR 均无法突破。

**固化成果**（`exp_finalize.py`，用户决策「固化 89%」）：
- `安居区_可疑标签清单.csv`：2292 条（小麦→油菜 1879、油菜→小麦 413），含 fid/原标签/建议标签/P_油菜/置信度，供人工复核。
- `小春_小麦油菜_清洗后模型.pkl`：清洗后全量重训模型（Acc 89.26%），含 label_encoder + feature_names + 参数。
- Top 特征全集中在 2025-03 时相（EVI/NDWI/B08/LSWI），印证「油菜靠 3 月花海、小麦靠返青」区分。

**下一步（若真要 95%）**：走「高分影像做边界 + 哨兵做类别」路线，用亚米级 DOM 补田埂/边界信息，纯哨兵光学已到顶。

**补充（08-24 晚）：花敏感指数 + WRI 验证（`exp_flower_indices.py` / `exp_finalize_v2.py`）**

检索公开文献后，补测了针对「油菜开花黄花」的花敏感指数，其中 **WRI（冬油菜指数，Tao 2019）** 效果显著：

- 公式：`WRI = (NIR-Green)/(NIR+Green) × Blue/(Green+Red)`，仅需 B02/B03/B04/B08 四波段，**无需新数据**（全在现有 GEE CSV 里）。
- 特征重要性：**WRI_2025-03 稳居 Top1/Top2**（gain 77k/73k），远超 NDYI_3月(57k) 和 EVI_3月(12k)——证明 WRI 是小麦/油菜二分类的最强单特征，印证文献「花敏感指数优于生物量指数」。
- 结果：基线 81.41% → +WRI 81.91%（+0.5pp）→ **+WRI+标签清洗 89.89%**（此前纯 172 维清洗为 89.26%，WRI 再 +0.63pp）。
- 小麦 recall 89.1%/precision 93.5%，油菜 recall 91.1%/precision 85.3%。

**结论**：WRI 是有效且「零成本」的特征（无需新数据），已固化进最终模型 `小春_小麦油菜_清洗后模型.pkl`（232 维 = 172 + 60 花敏感），可疑清单更新为 2403 条。但整体仍受 89~90% 物理天花板约束，95% 依旧需高分影像。

### 11.10 梓潼县小春跨市推理（无真值，08-24）

**背景**：用户要求拿 89.89% 小麦/油菜模型跑绵阳梓潼（重点清单县，需水稻/玉米/小麦/油菜 + 蜜柚）。核查发现**梓潼现有标注只有大春（水稻 8546 + 制种水稻 6435 + 玉米 3030），无小麦/油菜**，绵阳 5 县均如此，故无法做精度验证。

**用户决策**：无真值推理出图。

**流程**（`gee_extract_zitong.py` + `predict_zitong.py`）：
- GEE 提取梓潼 18011 块地块边界的 4 期小春特征（12/1/3/5月 × 10波段，不过滤 ZWMC，7 批完成）。
- 89.89% 模型推理（特征 232 维一致，无 NaN）。

**结果**：
- 预测小麦 14939 块（83.0%）、油菜 3072 块（17.0%）。
- 置信度：高(>0.9) 12333、中(0.7~0.9) 2591、低(<0.7) 3085 块。
- 产出：`梓潼县_小春预测.csv`（47 列，含 P_小麦/P_油菜/置信度）、`梓潼县_小春预测.shp`（加「小春预测」「小春置信度」字段）。

**重要 caveat**：模型是封闭二分类（小麦/油菜），会把**冬闲田/蔬菜等非小麦油菜地块也强行归入小麦或油菜**；低置信度 3085 块（17%）很可能就是这类地块。此结果仅作初步分布参考，不能当精度成果；若要可靠，需补梓潼小春真值标注（人工或参考 CN_Wheat10/CARM30 成品图）。

### 11.11 梓潼成品图交叉复验（CN_Wheat10 / CARM30，08-24）

**目的**：用公开成品图给梓潼推理结果一个参考验证。

**数据获取**：
- CN_Wheat10（武大 10m 小麦图，figshare 10.6084/m9.figshare.28852220）：下载 2024 年 668MB rar，解压得 `CN-Wheat10_2024_WW_H.tif`（冬小麦收获 2.16GB，15省）+ `WW_P`（种植 0.92GB，10省）。四川（SC）确在 15 省覆盖内。
- CARM30（武大 30m 油菜图，Mendeley 10.17632/hxhkphgmtt.1）：**被 Cloudflare 拦截，无法匿名下载**，油菜复验未成。

**关键发现——全国成品图对四川丘陵严重漏检**：
- CN_Wheat10 编码为 `1=小麦, 255=nodata`（无「0=非小麦农田」）。
- 梓潼窗口内 **96.2% 像元为 nodata，仅 3.8% 为小麦**；对应地块层面 **14348/18011 块（80%）完全无有效数据**。
- 根因：四川在 CN_Wheat10 覆盖边缘，且丘陵区 Sentinel-1/2 影像缺失导致制图不完整（同 WinterWheat-CN10m 已知问题）。

**唯一有意义的正向结果**：在成品图明确标出小麦的 3663 块地块里，模型 recall=**93.26%**（precision 无参考意义，因成品图无「非小麦」标注）。即我们的模型与独立第三方（精度 0.93）在「小麦」上高度一致。

**结论**：成品图交叉复验价值有限——80% 地块无覆盖，只能对 20% 地块做小麦 recall 验证（93.3%）。**本地无真值 + 全国成品图丘陵漏检，梓潼小春精度仍无法可靠验证；若要可靠复验，唯一现实路径是人工抽查（对照影像目视判读几十块）**。

### 11.12 作物识别能力盘点（08-24）

按「能否识别」分四档，这是当前整体能力边界：

**A. 已具备、可用的识别能力（均为二分类）**

| 能力 | 类别 | 精度 | 依赖信号 |
|------|------|------|---------|
| 大春水稻/玉米二分类 | 水稻 vs 玉米 | 23县留一县 89.9%；绵阳迁移 83.3%（盐亭92.9/平武92.1/三台88.7/梓潼80.4/江油67.7） | 水分/物候（B11+NDWI，水稻淹水 vs 玉米旱地） |
| 小春小麦/油菜二分类 | 小麦 vs 油菜 | 清洗后 89.9%，物理天花板 ~90% | 3月花敏感指数（WRI/NDYI 油菜开花变黄） |

**B. 已尝试、物理不可行**

- 果园 vs 林地：常绿柑橘园 vs 常绿天然林在 10m 哨兵光学上无法区分（冬季 NDVI 都高，只能学到「落叶 vs 常绿」）。
- 品种级识别（柑橘/蜜柚品种、李子/柑橘品种）：不可行，数据与信号都不支持。

**C. 有标注但未建模**

- 高粱：2,154 块（大春标注库），但被当「其他/剔除」处理、未单独建类（绵阳任务清单无高粱）。

**D. 无标注、无模型（完全未覆盖）**

- 蔬菜、柑橘（作为独立类）、蜜柚、中草药。

**总判断**：目前真正落地的能力是**两个季节各自的二分类**——大春「水稻/玉米」、小春「小麦/油菜」，恰好覆盖《作物识别要求》里的 4 个主要作物，但均为二分类（需分季节分别跑，不能一次区分四类），且距 95% 仍有差距。其余作物（蔬菜/柑橘/蜜柚/中草药/高粱）基本未做。**关于 0.5m 田埂与一分地最小上图：10m 哨兵先天达不到，必须走「高分影像做边界 + 哨兵做类别」路线**。

---

## 十二、用地识别试点（前进村，08-24）

### 12.1 背景与数据配对

《用地识别要求》是**用地类型分类**（水田/旱地/园地/林地/建设用地等），不是作物识别。主体必须靠 <0.8m 高分影像，哨兵 10m 只能做辅助。现有数据恰好达标且配对完整：

| 数据 | 来源 | 说明 |
|------|------|------|
| 高分影像 | `F:\YXX\江油市影像\0715江油马角镇-1.tif` | 8.9cm RGB（实为 3 波段，第4波段为 Alpha 全255） |
| 三调标注 | `F:\0421给yxx\提交成果\前进村\矢量\2前进.shp` | 1826 图斑 14 类（YDFLEJ 三调用地分类编码） |

前进村完全落在 0715 马角镇-1 场景内；6 村合并后共 7763 图斑 18 类。

### 12.2 试点方案

- 降采样 0.53m（8.9cm × 6），图斑 rasterize + bincount zonal 统计
- 特征：R/G/B + 可见光指数（ExG/NGRDI/VARI/GLI/gray）+ 纹理（grad），各 mean+std = 19 列
- LightGBM 多分类 + class_weight='balanced' + 5 折分层 CV
- 样本 ≥40 的类参与训练，共 9 类 1766 图斑（林地543/旱地387/宅基地216/园地184/交通161/水田94/水面76/其他建设54/草地51）

### 12.3 结果

**9 类细分类：Acc = 60.17%**（纯 RGB 对植被细类区分弱）

| 类别 | recall | 类别 | recall |
|------|--------|------|--------|
| 林地 | 84.3% | 草地 | 3.9% |
| 农村宅基地 | 77.8% | 其他水面 | 19.7% |
| 旱地 | 62.4% | 其他建设用地 | 13.0% |
| 交通运输 | 61.9% | 园地 | 26.1% |
| 水田 | 25.5% | | |

**4 大类合并：Acc = 76.65%**（农用地/林地/水域/建设用地）

| 大类 | recall | n |
|------|--------|---|
| 林地 | 82.3% | 542 |
| 建设用地 | 79.3% | 430 |
| 农用地 | 77.5% | 712 |
| 水域 | 13.2% | 76 |

### 12.4 结论

1. **9 类细分类不可用**（60%）：纯可见光 RGB 三波段对植被细类（水田 vs 旱地、园地 vs 林地）区分能力先天不足；错误集中在水田↔旱地、园地↔林地、草地↔林地之间。
2. **4 大类预分类可用（76.7%）**：林地/建设用地/农用地三大类 recall 77%~82%，可作为「减少人工初筛」的自动预分类工具；水域样本少（76 块）且被大量误判，不可用。
3. **改进方向**：叠加哨兵 NIR/红边（B8/B5/B6/B7/B11/B12）区分植被细类，或引入地形（DEM 坡度/坡向）辅助水田/旱地/园地/林地划分。
4. **产出**：`landuse_train.py` + `landuse_qianjin_model.pkl`（9 类模型，4 大类由标签合并）。
5. **下一步候选**：扩展到印坪/大岳/沉水/马阁寺/龙宫等其余 5 村（7763 图斑）做跨村泛化验证。

### 12.5 六村合并训练（08-24）

确认 6 村均有马角镇影像覆盖：`F:\YXX\江油市影像` 共 14 景（0715~0721，均未写 CRS 但坐标=CGCS2000 高斯，与标注 EPSG:4544 数值一致）。6 村 extent 与 14 景影像并集比对：前进/沉水/大岳/马阁寺/龙宫覆盖 >100%（多景重叠），印坪村西端约 15% 无影像。

**数据量**：6 村合并 7763 图斑 18 类；覆盖≥80% 可提取 7692；有效 7677；样本≥40 的 11 类 7634 图斑。

**结果**（LightGBM，特征同前进村 19 列）：

| 口径 | Acc | 说明 |
|------|-----|------|
| 11 类细分类 5折CV | 45.2% | 比单村 60% 更差；水田 precision 仅 7.2%（class_weight 使小类被过度预测） |
| 四大类 5折CV | 77.9% | 农用地 recall 89.4%/prec 83.8%；建设用地 55.1%/80.3%；其他水域 12.2% |
| 四大类 按村留一 | 72.3% | 跨村泛化：农用地 77.7%/84.9%；建设用地 68.0%/50.4%；其他水域 7.9% |

**结论**：

1. 6 村合并训练后，**四大类（农用地/建设用地）精度与单村持平略升**（5折 77.9% vs 单村 76.7%），说明样本扩充对大类有帮助。
2. **跨村泛化有损失**（72.3%，建设用地 precision 从 80% 掉到 50%），意味着换到新镇/新村时建设用地误判会增多——但「农用地 vs 非农用地」的粗筛仍可用（农用地 precision 84.9%）。
3. **其他水域始终不可用**（5折 12%、跨村 8%）：水体在可见光 RGB 上与阴影、道路、屋顶混淆，样本仅 343 且本身分散。
4. **细分类不可用**：合并后跨村光谱漂移 + 类别不平衡，11 类反而比单村 9 类更差，明确放弃细类自动分类。

**最终定位**：这套「无人机 RGB + LightGBM」能稳定交付的只有**农用地/建设用地的大类粗筛**（农用地侧尤其可靠），可做「自动勾出非农用地（宅基地/道路/其他建设）+ 农用地」两类，把人工从逐地块勾绘降到只复核边界与少数误判。水域与所有细类仍需人工或另加 NIR/DEM 辅助。

**产出**：`landuse_train_multi.py` + `landuse_6village_model.pkl`（11 类细分类模型 + 四大类标签映射）。

### 12.6 叠加哨兵 NIR/红边（08-24）

无人机影像只有 RGB 三波段，是「农用地 vs 建设用地」大类精度上不去的根因。用 GEE 从 Sentinel-2 提取**同期（2025-07）8 波段**（B4/B5/B6/B7/B8/B8A/B11/B12）zonal mean，7763 图斑全提取，与无人机 RGB 特征按 fid 拼接重训。

**结果**（四大类，LightGBM）：

| 组合 | 5折Acc | 跨村Acc | 说明 |
|------|--------|---------|------|
| A 纯RGB（baseline） | 77.9% | 72.3% | 上一节结果 |
| B RGB + 哨兵8波段 | 84.7% | 83.2% | 哨兵原始波段已够用 |
| C RGB + 哨兵指数(NDVI/NDRE/NDWI) | 82.2% | 81.4% | 仅指数略弱于全波段 |
| **D RGB + 波段 + 指数** | **85.1%** | **83.4%** | 最优 |

**最优组合 D 按村留一（跨村泛化）细节**：

| 大类 | recall | precision | 对比纯RGB跨村 |
|------|--------|-----------|--------------|
| 农用地 | 91.2% | 87.6% | 77.7% / 84.9% |
| 建设用地 | 72.5% | 74.2% | 68.0% / **50.4%** |
| 其他水域 | 14.6% | 31.6% | 7.9% / 14.7% |

**结论**：

1. **哨兵 NIR/红边是关键增益**：四大类跨村泛化 Acc 从 72.3% 提到 83.4%（+11pt），5折从 77.9% 提到 85.1%。
2. **建设用地 precision 从 50.4% 提到 74.2%**——哨兵 NIR 把「植被(农用地)」和「非植被(建设用地/道路)」干净分开，消除了此前最大的一类误判（大量农用地被误判成建设用地）。
3. **农用地 recall 91.2%**，已相当可靠，可作「自动勾出非农用地 + 农用地」的粗筛主力。
4. 其他水域仍差（14.6%），水体在 RGB 与阴影/屋顶混淆的问题 NIR 只能部分缓解，仍需人工或另加 DEM/水体指数。
5. 波段原始值（B 组合）已接近最优，说明**只需补充哨兵 8 波段，无需复杂指数工程**。

**最终结论（用地识别试点闭环）**：无人机 RGB + 哨兵 NIR/红边/SWIR 融合后，**四大类跨村泛化达 83.4%，农用地/建设用地两大主体类可自动预分类**，能切实减少人工勾绘量；细类（水田/旱地/园地/林地）与其他水域仍不可用。

**产出**：`gee_extract_landuse.py`（GEE 提哨兵波段）+ `gee_6村_哨兵波段_202507.csv` + `landuse_train_fusion.py`（融合训练对比）。

---

## 十三、项目总结与成果盘点（08-24）

### 13.1 做了什么、完成了什么（时间线）

| 阶段 | 内容 | 结果 |
|------|------|------|
| 大春作物识别 | 23 县标注库（泸州/遂宁/雅安/宜宾 21 区县，90,301 块）训练水稻/玉米二分类 | 留一县 **89.9%**；绵阳 5 县迁移 **83.3%**（盐亭92.9/平武92.1/三台88.7/梓潼80.4/江油67.7） |
| 大春江油攻坚 | 江油跨带域（48R→48S）偏移诊断：云掩膜/微调/重训/类别平衡 5 法交叉验证 | 判定 **balAcc 60% 为域偏移上限**；SCL 云掩膜救水稻召回至 85% 但玉米崩 |
| 小春作物识别 | 转 GEE 云端提取（4时相×10波段）+ 花敏感指数（WRI/NDYI/GRVI/VARI/NDPI）+ 安居标签清洗 2403 条 | **小麦/油菜二分类 89.89%**（10m 哨兵光学天花板 ~90%） |
| 小春跨市推理 | 用 89.89% 模型跑梓潼（无真值） | 预测小麦 14939 块(83%)/油菜 3072 块(17%)，低置信 17% |
| 公开成品图复验 | CN_Wheat10 / CARM30 交叉复验 | CN_Wheat10 四川丘陵 96.2% nodata、80% 地块无覆盖，复验价值有限 |
| 用地识别试点 | 江油 6 村无人机 RGB（8.9cm）+ 三调标注，LightGBM | 单村 9 类 60%、4 大类 76.7%；6 村合并 11 类 45%、四大类 77.9% |
| 用地+哨兵融合 | GEE 提哨兵 NIR/红边/SWIR 8 波段，与无人机 RGB 拼接 | **四大类跨村泛化 83.4%**（农用地 recall 91.2%、建设用地 72.5%） |
| 果园识别 | 叙永柑橘果园探索 | **物理不可行**（常绿柑橘园 vs 常绿天然林在 10m 哨兵上无法区分） |
| 高粱 | 大春标注库有 2,154 块 | 有标注、未建模（绵阳任务清单无高粱） |

### 13.2 关键代码清单

**大春（水稻/玉米）**
- `train_dachun_binary.py` — 大春水稻/玉米二分类训练
- `extract_pixels_dachun.py` / `extract_pixels_v2.py` — 大春特征提取（像素级）
- `predict_dachun_binary.py` — 大春推理

**小春（小麦/油菜）**
- `gee_extract_features.py` — GEE 云端小春特征提取（4 时相 × 10 波段）
- `train_xiaochun_gee.py` — 小春训练
- `exp_finalize_v2.py` — 小春最终化（花敏感指数 + 标签清洗，产出 89.89% 模型）
- `exp_flower_indices.py` — 花敏感指数（WRI/NDYI/GRVI/VARI/NDPI）实验
- `exp_label_cleaning.py` / `_label_clean.py` — 安居区标签清洗
- `predict_zitong.py` — 梓潼无真值推理

**用地识别（无人机 RGB + 哨兵）**
- `landuse_train.py` — 前进村单村试点（9 类 + 4 大类）
- `landuse_train_multi.py` — 6 村合并训练（多影像归属 + 11 类 + 四大类 + 按村留一）
- `gee_extract_landuse.py` — GEE 提取哨兵 NIR/红边/SWIR 8 波段
- `landuse_train_fusion.py` — 无人机 RGB + 哨兵融合训练对比

**基础设施/文档**
- `GEE直连与影像使用操作文档.md` — GEE 直连、选景、提特征的完整操作说明
- `common.py` — 公共工具（影像/特征复用）

### 13.3 当前可交付的识别能力

**A. 作物识别（哨兵 10m 光学，GEE 云端，无需下载影像）**

| 能力 | 类别 | 精度 | 状态 |
|------|------|------|------|
| 大春二分类 | 水稻 vs 玉米 | 留一县 89.9%；绵阳迁移 83.3% | ✅ 可用（分季节单独跑） |
| 小春二分类 | 小麦 vs 油菜 | 清洗后 89.89% | ✅ 可用（分季节单独跑） |
| 高粱 | — | 有标注未建模 | ⚠️ 待补（绵阳清单无） |
| 蔬菜 | — | 无标注无模型 | ❌ 未覆盖 |
| 柑橘/蜜柚/中草药 | — | 无标注无模型 | ❌ 未覆盖 |
| 果园 vs 林地 | — | 光谱不可区分 | ❌ 物理不可行 |

> 说明：作物识别均为「分季节二分类」，不能一次区分四类；精度距 95% 目标仍有差距（哨兵 10m 光学天花板）。

**B. 用地识别（无人机高分 RGB + 哨兵 NIR 融合）**

| 能力 | 类别 | 精度（跨村泛化） | 状态 |
|------|------|------------------|------|
| 四大类粗筛 | 农用地 / 建设用地 / 其他水域 / 其他土地 | 整体 83.4%；农用地 recall 91.2%、建设用地 72.5% | ✅ 可用（农用地侧可靠） |
| 细类 | 水田/旱地/园地/林地/草地 | 45%~60% | ❌ 不可用 |
| 其他水域 | 水体 | 14.6% | ❌ 不可用 |

> 说明：用地识别只能交付「农用地 vs 建设用地」两大类粗筛，可自动勾出非农用地、减少人工逐地块勾绘；细类与水域仍需人工。

### 13.4 打包成品软件：用户需提供的数据输入

按两条产品线分述「训练（首次/增量）」和「推理（日常使用）」两种场景。

**产品线 1：作物识别（哨兵 10m）**

| 场景 | 必需输入 | 说明 |
|------|---------|------|
| 推理（日常） | ① 地块边界矢量 SHP | 含地块边界 + 唯一 fid（含 `ZWMC` 等字段更好，可做后校验）；坐标 WGS84 或可转 CGCS2000 高斯 |
| | ② GEE 账号/项目授权 | 哨兵影像在 GEE 云端提取，本机需 `ee.Initialize()` 通过（无需下载影像） |
| 训练（首次） | ③ 带标签的地块 SHP | 额外含作物标签字段（如 `ZWMC`=水稻/玉米/小麦/油菜），用于训练 |
| | ④ 影像年份 | 标签必须与哨兵影像同一年（轮作作物，否则张冠李戴） |

**产品线 2：用地识别（无人机高分 + 哨兵）**

| 场景 | 必需输入 | 说明 |
|------|---------|------|
| 推理（日常） | ① 高分影像（<0.8m） | 无人机/航拍 DOM，**RGB 三波段即可**（有 NIR 更佳）；带正确投影坐标 |
| | ② 地块边界矢量 SHP | 与影像同坐标系（本项目 EPSG:4544 / CGCS2000 高斯），含 fid |
| | ③ GEE 账号/项目授权 | 用于提取同期哨兵 NIR/红边/SWIR 波段 |
| 训练（首次） | ④ 带标签的地块 SHP | 额外含用地分类标签（如 `YDFLEJ`），用于训练 |
| | ⑤ 影像年份/时相 | 哨兵波段需与高分影像同期（本项目 2025-07） |

**通用输入（两类产品线都需要）**

1. **地块边界 SHP** — 一切识别的最小输入；训练时附加标签字段。
2. **GEE 授权** — 哨兵光谱/指数来源，替代「下载哨兵影像」这一重环节。
3. **影像年份/时相说明** — 保证影像与标签同年同季。

> 结论：**边界 SHP + 高分影像（用地）/GEE 授权（作物）** 是用户必须提供的数据源头；其余（哨兵波段、特征、模型）均可由系统自动生成。

### 13.5 哨兵影像获取模式：默认 GEE，本地兜底（08-24）

哨兵影像的获取有「GEE 云端」和「本地下载」两种模式，**模型效果一致**（都是 10m 哨兵波段 zonal 均值 + 同一套 LightGBM），差异只在工程/部署层：

| 维度 | GEE 云端 | 本地下载 |
|------|---------|---------|
| 模型效果 | 一致 | 一致 |
| 需要什么 | GEE 账号（Google 账号 + 代理 + 项目审批） | 无需账号，能访问哨兵下载源即可 |
| 磁盘占用 | 0（影像不落地，只回传特征 CSV） | 十几~几十 GB |
| 选景/云掩膜/合成 | GEE 统一 API 自动完成，规范可复现 | 自己写，选景与预处理易不一致 |
| 隐性风险 | 账号/网络依赖 | 下载中断、瓦片源不统一、复现难 |

**结论**：**默认走 GEE 云端**（省磁盘、免下载、统一数据集更可复现）；**本地下载作为兜底**，仅在「客户没有 GEE 账号 / 无法联网访问 GEE」时启用。本地兜底链路已有现成代码：`download_sentinel_v2.py` / `download_dachun_s2.py`（下载）+ `extract_pixels_v2.py`（本地提特征）。

**交付前唯一需确认项**：最终客户是否持有 GEE 账号并能联网访问 GEE。能 → 全走 GEE；不能 → 启用本地下载兜底。

### 13.6 高粱三分类试点 + 大春 GEE vs 本地对比（08-24）

**（对 13.5 的更正）**：经大春实测，13.5 中「GEE 与本地效果一致」需修正——当本地用「每期选云量最低单景」时，GEE 的 median 合成显著更优（见下）。若本地同样做 median 合成，二者才会趋同。

**高粱三分类试点**（射洪/泸县/龙马潭/船山/纳溪 5 县，高粱 2,154 块，全库占 2.4%）：
- 同县 5 折：高粱 recall 72% / precision 39%（本地）、46%（GEE），F1 0.50 / 0.57，precision-recall 无法兼顾（阈值 0.8 时 precision 0.74 但 recall 仅 0.42）。
- 跨县留一：主产区射洪留出后高粱 recall 仅 35%。
- **结论：高粱无法可靠识别**，物理信号太弱（高粱 vs 玉米同为夏季旱作，光谱物候几乎重合），不纳入交付。

**大春 GEE vs 本地对比**（同批 5 县地块）：

| 指标 | 本地单景 | GEE median |
|------|---------|-----------|
| 5 折 Acc | 0.8726 | 0.8860 |
| 留一县平均 Acc | 0.7628 | **0.8705** |
| 船山区（本地最差） | 0.495 | 0.873 |

- **结论：大春应走 GEE**，median 合成去云更干净，跨县泛化 +10.8 个百分点。

**高粱时相瓶颈与改进方向**：高粱靠「8 月上旬早收、玉米仍灌浆」区分，但当前 4 期（5/6/7/8 整月）把该短暂信号平均掉。改进方案见 13.6 节后讨论——核心是 GEE 加「8 月上旬」细窗口 + NDVI 骤降特征。

**产出**：`extract_gaoliang.py`、`train_gaoliang.py`、`gee_extract_dachun.py`、`train_dachun_gee_vs_local.py`、`gaoliang_features.csv`、`gee_dachun_5counties.csv`。

### 13.7 时相选择验证 + 高粱定性 + 水稻/玉米二分类（08-24）

**时相选择验证**（GEE 7 期细时相：4/5/6/7/8上/8下/9 月，5 县）：

| 组合 | 三分类 Acc | 水稻F1 | 玉米F1 | 高粱F1 | 高粱precision |
|------|-----------|--------|--------|--------|--------------|
| 5/6/7/8上 (4期) | 0.880 | 0.913 | 0.895 | 0.537 | 0.427 |
| +8下 (5期) | 0.892 | 0.918 | 0.905 | 0.596 | 0.491 |
| +4月 (6期) | 0.893 | 0.922 | 0.905 | 0.599 | 0.495 |
| +9月 (7期=4~9全) | 0.900 | 0.923 | 0.911 | 0.626 | 0.525 |

- **时相越密，三分类总 Acc 越高**（0.880 → 0.900），但提升主要来自「高粱」被更好分开。
- **纠正原假设**：高粱 vs 玉米的差异主要在「8 月下旬~9 月收获期」，而非之前假设的「8 月上旬早收」。+8 下旬对高粱 F1 贡献最大（+5.9%）。

**方案 A+B**：A（加时相）有效；B（手工「NDVI 骤降」早收特征）无效——LightGBM 从原始波段已能自学到 7→8 月 NDVI 变化（`compute_feature_matrix` 本就含 `DELTA_NDVI`/`senescence_rate`）。

**水稻/玉米二分类（7 期，5 县）**：

| 组合 | 5折 Acc | 留一县 Acc |
|------|---------|-----------|
| 4期 5/6/7/8上 | 0.929 | **0.933** |
| 5期 5/6/7/8上/8下 | 0.930 | 0.925 |
| 7期 4~9月全 | **0.933** | 0.921 |

- **结论：对水稻/玉米二分类，时相加密收益很小**（同县 5 折仅 +0.4%），跨县留一甚至略降（-1.2%，时相越密越易跨县过拟合）。4~5 期已够，不值得为水稻/玉米单独加密时相。

**高粱最终定性**：即使 7 期，precision 仅 0.525（预测为高粱的地块近半误报），仍不纳入交付。

**根本问题（用户洞察）**：时相选择的准确性取决于「目标区县的实际农时」，当前用四川通用物候预设窗口，可能漏关键时相。解法方向：① GEE 数据驱动密集时相（旬级滑动窗口，让模型自选关键期）；② 农时调研（目标县实际播种/收割时间）；③ NDVI 时间序列物候拐点自动检测。

**产出**：`gee_extract_dachun_7phase.py`、`train_gaoliang_5phase.py`、`train_dachun_binary_7phase.py`、`gee_dachun_5counties_7phase.csv`。

### 13.8 数据驱动的时相自选验证（08-31）

**方法**：把 2025 年 4~9 月切成 **18 个旬**（每旬 10 天，D01=4月上 ~ D18=9月下），对 5 县 42,012 块（水稻 18,307 / 玉米 21,551 / 高粱 2,154）做 GEE 提取，每旬 10 波段 = **180 列**。LightGBM 三分类，① 按旬聚合特征重要性（gain）看模型自选关键期；② 前向选择（按旬重要性从高到低累加）看 Acc 饱和点。

**先看数据缺失**（影响结论可信度）：

| 旬 | 含 NaN 样本比例 | 说明 |
|----|----------------|------|
| D13 8月上 / D14 8月中 | **0.0%** | 云量最少，最完整 |
| D11 7月中 | 3.2% | 较完整 |
| D12 7月下 / D15 8月下 | 15.5% / 22.8% | 尚可 |
| D01~D03 4月 | 45.7% ~ 69.9% | 春季云量偏高 |
| D07 6月上 | 83.7% | 雨季 |
| **D05 5月中 / D08 6月中 / D18 9月下** | **97.5% ~ 99.8%** | **几乎全被云覆盖，重要性为 0 是伪结论** |

**模型自选的关键旬（gain 重要性降序）**：

| 排名 | 旬 | 占比 | 物候含义 |
|------|----|------|----------|
| 1 | D11 7月中 | 22.7% | 营养生长旺盛期，冠层/水分差异最大 |
| 2 | D04 5月上 | 15.3% | 水稻移栽淹水 / 玉米出苗 |
| 3 | D14 8月中 | 12.7% | 成熟期，高粱率先转黄收获 |
| 4 | D10 7月上 | 10.1% | 生长盛期 |
| 5 | D09 6月下 | 5.8% | 分蘖/拔节 |
| 6 | D16 9月上 | 5.2% | 收获后茬口 |

**前向选择 Acc 饱和点**：

| 旬数 | 组合 | 三分类 Acc |
|------|------|-----------|
| 3 旬 | D11+D04+D14 | 0.875 |
| 5 旬 | +D10+D09 | 0.889 |
| 7 旬 | +D16+D12 | **0.899** |
| 9 旬 | +D13+D02 | 0.908 |
| 15 旬 | … | 0.923 |
| 18 旬 | 全部 | **0.922** |

**结论**：

1. **时相加密有真实收益**：18 旬全量 0.922 vs 月级 7 期 0.900，**+2.2%**。旬级（10 天）时间分辨率更高，减少了月级 median 的跨时段混合。
2. **模型自选的关键旬高度集中**：5 月上 + 7 月上/中 + 8 月中 贡献了 >60% 重要性，验证了「4~9 月」窗口方向正确，但真正的信息量集中在 **7 月中旬（生长旺盛期）**，而非均匀分布。
3. **top7 旬（0.899）≈ 月级 7 期（0.900）**：用 7 个「低云量」旬即可追平 7 个月级合成，说明之前 7 期的瓶颈不在旬数，而在 6 月等雨季旬数据质量差。
4. **局限**：6 月中旬（D08）几乎全被云覆盖，重要性被严重低估；若对雨季旬改用「多景去云合成 / 插值」补齐，18 旬上限还有上升空间。

**对时相配置的落地建议**：大春水稻/玉米/高粱优先取 **5 月上旬、7 月上/中旬、8 月中旬** 三个低云量旬作为核心时相；雨季（6 月）宁可少取、不可用单景硬凑（云污染反而引入噪声）。

**产出**：`gee_extract_dachun_18d.py`、`train_dachun_18d_select.py`、`gee_dachun_5counties_18d.csv`（42,012 × 183）。

### 13.9 18 旬上限试探：时序插值 vs 多景去云合成（08-31）

**背景**：13.8 发现雨季旬（5月中 / 6月 / 9月下）缺失率 96%~99.8%，怀疑拖累 18 旬上限，用两种手段补齐看能否突破 0.922。

**结果对比**（全量 18 旬三分类，5 折 Acc）：

| 方案 | 雨季旬 NaN | 18旬 Acc | 结论 |
|------|-----------|----------|------|
| 严格 10 天窗口（NaN 交 LightGBM） | 99.8% | **0.9228** | 基线 |
| 时序插值补齐（本地线性插值） | 0% | **0.9140** | 有害 **-0.9%** |
| 扩窗多景去云合成（GEE 30 天窗口） | 1.4%~51% | **0.9258** | 微升 **+0.3%** |

**关键发现**：

1. **时序插值有害**：线性插值用相邻旬平滑值「编造」了 6 月关键期光谱，破坏了真实物候拐点；LightGBM 原生处理 NaN（把「缺失」本身当特征）反而更稳。→ 不要对光学时序做插值补齐。
2. **扩窗能大幅降 NaN，但 Acc 收益微薄**：D05 5月中 99.8%→1.4%、D08 6月中 97.5%→32.5%、D03 4月下 69.9%→0.8%，而全量 Acc 仅 +0.3%（0.9228→0.9258）。且扩窗旬与相邻旬时间重叠，牺牲了旬级时间分辨率、模糊了「关键旬」含义。
3. **18 旬上限 ≈ 0.922~0.926**：瓶颈不是数据缺失，而是水稻/玉米/高粱在旬级光学光谱上的**可分性物理天花板**。

**物理解释**：6 月虽是生长关键期，但三类作物此时都是旺盛生长的绿色植被，光谱差异很小；真正区分信号在 5 月（水稻淹水）和 8~9 月（收获期差异）——恰好就是模型自选的关键旬。所以补雨季数据对三分类贡献甚微。

**落地结论**：大春时相按 **5 月上、7 月上/中、8 月中** 三个低云量旬取即可，无需为雨季旬做扩窗或插值（插值有害、扩窗收益微薄）；「严格 10 天窗口 + LightGBM 原生 NaN」已是最优实践。

**产出**：`train_dachun_18d_interp.py`、`gee_extract_dachun_18d_mc.py`、`train_dachun_18d_mc.py`、`gee_dachun_5counties_18d_interp.csv`、`gee_dachun_5counties_18d_mc.csv`。

### 13.10 关键 3 旬重训水稻/玉米二分类（08-31）

**目的**：把 13.8 数据驱动自选的「关键 3 旬」直接落地到水稻/玉米二分类，验证是否比原来的 4 期/7 期更精简且不降精度。

**关键 3 旬** = 5月上(D04) + 7月中(D11) + 8月中(D14)，旬级 18d 数据 + `compute_feature_matrix` 5 层特征。

| 组合 | 5折Acc | 5折F1 | 留一县Acc | 特征数 |
|------|--------|-------|-----------|--------|
| **关键3旬 5月上+7月中+8月中** | 0.928 | 0.928 | **0.930** | 154 |
| 关键3旬 +7月上 | 0.931 | 0.931 | 0.920 | 172 |
| 关键3旬 +8月下 | 0.931 | 0.931 | **0.887** | 172 |
| 全18旬（上界） | 0.934 | 0.934 | 0.925 | 424 |

对照（13.7 月级）：4期 5/6/7/8上 = 0.929 / 留一县 0.933；7期 4~9全 = 0.933 / 留一县 0.921。

**结论**：

1. **关键 3 旬就够**：5折 Acc 0.928、留一县 0.930，与 4 期（0.929/0.933）持平，**优于 7 期留一县（0.921）**，且只用 154 维特征（比 4 期的 172 维还少）。
2. **加旬反而有害**：关键 3 旬 +8月下，留一县暴跌到 0.887——8 月下旬水稻/玉米都处于成熟期、光谱趋同，加该旬对二分类只引入噪声（与三分类里「8月下是高粱收获期」的作用相反）。
3. **落地配置定型**：大春水稻/玉米二分类的最优时相 = **5月上旬 + 7月中旬 + 8月中旬** 三个旬级窗口；无需 6 月（雨季低信息量）、也无需 8 月下/9 月（成熟期光谱趋同）。

**产出**：`train_dachun_binary_3dekad.py`。

### 13.11 代码库全面排查：当前主线代码版本与输入输出（08-31）

> 背景：时相验证做到一半，发现"现在到底沿用哪套代码、每个脚本输入输出是什么"已经记不清，故对主目录代码做一次全面排查，固化成清单，避免后续软件化时拿错脚本。

#### 一、代码库结构

| 位置 | 数量 | 性质 |
|------|------|------|
| 主目录 `*.py` | 42 个 | 当前代码（含主线 + 少量实验脚本） |
| `_archive/` | 160+ 个 | 历史废弃/实验脚本（已归档，不再使用） |
| `果树识别_step1_coldstart/` | 子目录 | 果园识别独立脚本（果园已定性不可行） |
| `江油_prediction/`、`dachun_prediction/` | 目录 | 预测输出数据（非代码） |

#### 二、当前技术路线总览

**核心结论：走 GEE 云端路线**（影像在 GEE 云端，本地只跑特征 CSV 的训练与推理），但**三条产品线的"GEE 化"程度不一致**：

| 产品线 | 提取 | 训练 | 预测 | GEE 化程度 |
|--------|------|------|------|-----------|
| 大春（水稻/玉米） | GEE ✅ | GEE ✅ | ❌ 仍读本地瓦片 | **半打通** |
| 小春（小麦/油菜） | GEE ✅ | GEE ✅ | GEE ✅ | **全打通** |
| 用地分类 | GEE 提哨兵波段 ✅ | 无人机 RGB + 哨兵融合 ✅ | 未单独封装 | 半打通 |

#### 三、主线代码输入输出对照表

**① 大春（水稻/玉米）— 提取（GEE 云端，均读 `大春训练标注库_完整字段.gpkg` 的 5 县地块）**

| 脚本 | 时相 | 输出 CSV |
|------|------|----------|
| `gee_extract_dachun.py` | 4期（5/6/7/8月）×10波段 | `gee_dachun_5counties.csv` |
| `gee_extract_dachun_7phase.py` | 7期（4~9月）×10波段 | `gee_dachun_5counties_7phase.csv` |
| `gee_extract_dachun_18d.py` | 18旬（每旬10天）×10波段 | `gee_dachun_5counties_18d.csv` |
| `gee_extract_dachun_18d_mc.py` | 18旬扩窗多景去云 | `gee_dachun_5counties_18d_mc.csv` |

**② 大春（水稻/玉米）— 训练**

| 脚本 | 输入 | 输出 | 结论 |
|------|------|------|------|
| `train_dachun_binary.py` | `待训练数据大春/features_23counties.csv`（本地提取的 23 县特征） | `dachun_binary_23counties/crop_model_binary.pkl` | 23 县模型，留一县 89.9% |
| `train_dachun_binary_7phase.py` | `gee_dachun_5counties_7phase.csv` | — | 7 期二分类，5折 0.933 |
| `train_dachun_binary_3dekad.py` | `gee_dachun_5counties_18d.csv`（取关键3旬） | — | **最新：关键3旬 0.928/留一县 0.930** |
| `train_dachun_gee_vs_local.py` | `gee_dachun_5counties.csv` | — | GEE 优于本地单景 +10.8% |

**③ 大春（水稻/玉米）— 预测（⚠️ 未 GEE 化）**

| 脚本 | 输入 | 输出 |
|------|------|------|
| `predict_dachun_binary.py` | 本地瓦片影像（`E:\迅雷下载`，4期）+ `crop_model_binary.pkl` + `tile_dates.json` | `dachun_prediction/` 下 shp/csv；三模式：`mianyang`/`qianjincun`/`anju` |

**④ 小春（小麦/油菜）— GEE 全打通**

| 脚本 | 输入 | 输出 |
|------|------|------|
| `gee_extract_features.py` | 小春标注 shp（过滤小麦/油菜） | `gee_*.csv`（4期：12/1/3/5月×10波段） |
| `train_xiaochun_gee.py` | `gee_*.csv` | 小春模型（二分类） |
| `exp_finalize_v2.py` | 小春特征 | `小春_小麦油菜_清洗后模型.pkl`（89.89%） |
| `gee_extract_zitong.py` | `梓潼县.shp`（不过滤，全地块） | `gee_梓潼县_小春特征.csv` |
| `predict_zitong.py` | 模型 pkl + `gee_梓潼县_小春特征.csv` | 梓潼预测 CSV + 写回 SHP |

**⑤ 用地分类（无人机 RGB + 哨兵 NIR 融合）**

| 脚本 | 输入 | 输出 |
|------|------|------|
| `gee_extract_landuse.py` | 6 村图斑 shp（`F:\0421给yxx\提交成果`） | 哨兵 8 波段（红边/NIR/SWIR）zonal mean |
| `landuse_train.py` | 前进村单村无人机 RGB 图斑 | 单村 9 类/4 大类模型 |
| `landuse_train_multi.py` | 6 村无人机 RGB 图斑 | 6 村合并 11 类/四大类（留一村） |
| `landuse_train_fusion.py` | 无人机 RGB + 哨兵波段 | 四大类跨村 83.4% |

**⑥ 其他（定性结论已固化，脚本可归档）**

| 脚本 | 作用 | 结论 |
|------|------|------|
| `extract_gaoliang.py` / `train_gaoliang.py` / `train_gaoliang_5phase.py` | 高粱三分类试点 | 高粱无法可靠识别（precision 0.525），不纳入交付 |
| `extract_pixels_dachun.py` / `extract_pixels_v2.py` | 像素级训练数据提取 | 像素级兜底链路 |
| `raster_to_shp.py` / `vectorize_anju_cropmap.py` / `vectorize_qianjincun_cropmap.py` | 像素预测栅格矢量化 | 矢量化成地块 shp |
| `train_dachun_18d_select.py` / `_interp.py` / `_mc.py` | 18 旬时相自选/插值/多景 | 结论已入 13.8~13.10 |
| `_label_clean.py` / `_label_clean_mianyang.py` | 标签清洗 | 一次性脚本 |
| `download_dachun_s2.py` / `download_sentinel_v2.py` / `download_sentinel1_v2.py` | 本地下载（兜底） | 仅客户无 GEE 时启用 |
| `common.py` | `compute_feature_matrix` 5 层特征工程 | **核心公共库**，所有训练/预测依赖 |

#### 四、关键发现与待办

1. **【核心缺口】大春预测环节未 GEE 化**：训练已走 GEE（median 去云，跨县 +10.8%），但 `predict_dachun_binary.py` 仍读本地单景瓦片——训练用 GEE median、预测用本地单景，两者特征口径不一致，是当前最大的版本割裂点。小春已完整 GEE 化，可作为改造模板。
2. **待办**：仿照小春链路（`gee_extract_zitong.py` + `predict_zitong.py`），给大春补一个 GEE 预测脚本：读地块 shp → GEE 提关键 3 旬特征 → 模型预测 → 写回 shp，与训练口径对齐。
3. **大量实验脚本可归档**：18 旬时相自选/插值/多景、高粱、以及主目录里 `exp_*.py` 实验脚本，结论已固化进工作记录，可移入 `_archive/` 减少主目录噪音。

**产出**：本节（13.11 代码库排查清单）。

### 13.12 大春 GEE 预测脚本补齐 + 重训 23 县关键 3 旬模型（09-02）

> 补上 13.11 发现的最大缺口：大春预测未 GEE 化。按「先重训关键 3 旬 23 县模型」顺序，补齐「提取 → 重训 → 预测」三步，全部走 GEE 云端，与训练口径完全一致。

#### 一、新增/更新脚本（3 个）

| 脚本 | 作用 | 输入 | 输出 |
|------|------|------|------|
| `gee_extract_dachun_23counties_3dekad.py` | 23 县库关键 3 旬 GEE 提取 | `待训练数据大春/大春标注_水稻玉米.gpkg`（类别 in 水稻/玉米，22 县 87521 块） | `gee_dachun_23counties_3dekad.csv`（30 波段 + fid/类别/QXMC） |
| `train_dachun_binary_23counties_3dekad.py` | 关键 3 旬重训 + 保存模型 | `gee_dachun_23counties_3dekad.csv` | `dachun_binary_23counties_3dekad.pkl` |
| `predict_dachun_gee.py` | 大春 GEE 预测（地块级） | 外部地块 shp（任意 CRS） + 模型 pkl | 写回 shp/gpkg/csv，含 11 个质量字段 |

#### 二、重训结果（23 县全量，关键 3 旬 = D04 5月上 + D11 7月中 + D14 8月中）

| 指标 | 值 |
|------|-----|
| 样本 | 87521（玉米 52564 / 水稻 34957），清洗后 77273（去除 NaN/异常 10248） |
| 5 折 CV | Acc=0.9037（±0.0014），F1=0.9033 |
| 留一县（LOGO） | Acc=0.8767（清洗后剩 19 县） |
| 特征数 | 154 |

> 说明：23 县全量比 5 县关键 3 旬（0.928/0.930）低，属预期——23 县含古蔺/叙永/天全/汉源等山区，跨县泛化更难。留一县从 22 县降到 19 县，是因为 `大英市`（19 块，疑似「大英县」笔误）等小县样本在清洗时被整县剔除。`大英市` 笔误待数据侧核实。

#### 三、预测脚本（`predict_dachun_gee.py`）要点

- 与训练完全一致的 GEE 口径：S2_SR_HARMONIZED + SCL 云掩膜（3/8/9/10）+ median 合成 + `reduceRegions(mean, scale=10, tileScale=4)`。
- 输入外部地块 shp 自动转 WGS84 供 GEE，面积按投影/UTM 计算，保留原 CRS 与全部原始字段。
- 输出 11 个质量字段（人工复核参考）：`parcel_id / area_m2 / ndvi_max / ndvi_flag / predicted / prdct_lbl / max_prob / conf_flag / prdct_cf / buf_pixel / px_statu`。
- 三阈值联合判定链（口径同 8.13）：`buf_pixel<1→无覆盖`、`ndvi_max<0.30→非植被(建筑/道路/裸土)`、`max_prob<0.60→不确定`，否则 `prdct_lbl`。
- `buf_pixel` = 三旬 B8 掩膜**并集**的 `ee.Reducer.sum().unweighted()`（任一旬有有效像元即算覆盖，整数像素）。
- 已用安居区 80 块样本端到端验证通过（prdct_cf：玉米 67/水稻 8/不确定 5；buf_pixel 整数）。

#### 四、SLIC 分割 + 套合的定位（待实现，独立质检模块）

用户方向：走「面向对象（SLIC 分割）预测」→ 套合到「地块级（外部给边界）」，两条路线结合做交叉质检。**结论：可行，定位为交付前质检模块，不阻塞主路径**。

- **主路径**：外部给边界 → `predict_dachun_gee.py` 直接逐地块预测（本次已补齐）。
- **质检路径**：哨兵全图 SLIC 分割 → 面向对象预测 → 用「最大重叠面积」套合到外部地块边界 → 比对两侧标签，输出差异清单。
- **能查的三类问题**：
  1. 我方漏预测（SLIC 有、外部地块没匹配上 → 甲方边界漏给/错位）；
  2. 我方多预测（外部地块内有多个 SLIC 且标签冲突 → 地块内异质/混种）；
  3. 对方边界少给（SLIC 检出作物但外部无对应地块 → 甲方漏地块）。
- **约束**：SLIC 超像素 ≠ 真实田埂（会切碎/粘连）；套合需设重叠面积阈值与容差（8.14 前进村 2291 块套合、未匹配 101 块的经验）；判定「少给/多给」要给面积比例阈值，不能一刀切。

**产出**：3 个脚本 + `gee_dachun_23counties_3dekad.csv` + `dachun_binary_23counties_3dekad.pkl` + 本节。

### 13.13 输出A/输出B 最终口径：两条都走 SLIC 分割（09-02）

> 用户拍板：输出A（主交付）与输出B（质检附件）**都走「SLIC 分割 → 对象/像元预测 → 套合」**，不做「外部地块直接 zonal mean」。

#### 一、最终架构

- **共同前置**：哨兵影像 → SLIC 分割 → 对每个分割对象预测作物（或地类）。
- **输出A（主交付）**：预测结果逐块套合赋值到外部 shp → 输出「外部 shp + 作物标签 + 11 个质量字段」。
- **输出B（质检附件）**：预测结果与外部 shp 套合对比 → 标注三类问题。
- 预测单元永远是「我方分割出来的对象」；外部 shp 在 A 里是「接收赋值」、在 B 里是「被对比」，不参与预测。

#### 二、讨论结论（关于「这样预测准不准」）

- 面向对象 + 套合是遥感作物分类主流方向，方向正确（8.12 实证：面向对象 44.4% vs 像素盲测 2.1%）。
- 但现有 23 县模型是「地块级 zonal mean」训练；SLIC 超像素比训练地块小、平均噪声更大、会把田切碎（SLIC ≠ 真实田埂），单对象预测稳定性弱于「地块直接平均」。
- SLIC 的主要价值在**质检/发现异质**（分离混种、查对方漏给/多给），不在提升主预测精度。
- 用户权衡后决定：输出A 仍走 SLIC，实现时 **SLIC 分割粒度调到接近田块尺度**，避免过碎导致精度反降。

#### 三、三类问题（外部边界为准前提下）

1. **我方漏预测**（问题在我方）：外部给了地块，我方没识别出作物（判成 非植被/无覆盖/不确定，或 SLIC 没分割出）。
2. **我方多预测 = 对方边界少给**（问题在甲方给的数据，同一空间现象两面）：我方识别出的作物区域落在外部边界之外 → 甲方漏给地块 / 边界偏差。
3. **地块内异质/混种**（需人工复核）：一个外部地块内多个 SLIC 对象标签冲突。

#### 四、赋值规则（待最终确认）

- 面积多数投票：占该地块面积最大的 SLIC 标签 = 地块标签。
- 最大标签面积占比 < 50%（默认）→ 输出B 标「地块内异质/混种」，输出A 标「不确定」。

#### 五、代码调整点（盘点）

- 主目录**无 SLIC/SNIC 分割脚本**、**无套合（overlay/最大重叠匹配）脚本**（8.13「面向对象」当时为临时脚本，已不在主线），需从零新增。
- `predict_dachun_gee.py`（外部地块 zonal mean）降级为兜底/对照，不作为输出A。
- 详细调整清单见本轮回复正文。

**产出**：本节（13.13 架构定型）。

### 13.14 SLIC 分割 + 套合链路实现与前进村验证（09-02）

#### 一、SNIC 分割粒度实验（前进村）

- 前进村耕地中位 1252m²（13px），训练地块中位 1749m²（17.5px），两者皆细碎（10m 分辨率下十几像素级）。
- SNIC `size` 扫描：`size=3→8px / 4→13px / 5→21px / 6→30px / 8→52px`。
- 定 **`size=4`**（超像素中位 13px，对上前进村真实田块尺度）；`compactness=5, connectivity=8, neighborhoodSize=256`。
- 分割底图：D11（7月中）median 真彩色（B4/B3/B2）+ NDVI。

#### 二、脚本产出（3 个）

1. `gee_slic_segment_extract.py`：GEE 端 SNIC 分割 → `reduceToVectors` 矢量化 → 提取关键3旬 30 波段 zonal mean + `buf_pixel`。
2. `predict_slic_overlay.py`：本地预测（复用 `predict_dachun_gee.run_predict`）+ 套合（输出A 赋值 / 输出B 质检）。
3. `experiment_snic.py`：SNIC 粒度实验（临时脚本）。

#### 三、前进村端到端验证（15963 超像素 → 386 外部地块）

输出A 套合赋值准确率（vs dltb 真实 DLMC）：

| 真实地类 | 地块数 | 判对 | 准确率 |
|---------|-------|------|--------|
| 旱地(玉米) | 286 | 236 | **82.5%** |
| 水田(水稻) | 90 | 32 | **35.6%** |

输出B 套合状态：正常 3331 / 地块内冲突 619 / 未匹配(非作物) 2814 / 未匹配-疑似甲方漏给 9199。

#### 四、发现的问题（待解决）

1. **水稻识别率低（35.6%）**：前进村为跨区域迁移场景 + 训练类别不平衡（玉米 52564 / 水稻 34957），水稻 recall 偏低。
2. **二分类模型把非耕地植被误判玉米**：验证 9199 个"未匹配作物"超像素，落在乔木林地 2416 / 灌木林地 370 / 其他林地 126 / 园地 51 / 草地 21 等——林地/园地被误判玉米，导致输出B"对方漏给"虚高。
3. **ROI 矩形超界**：SNIC 用 bbox 矩形分割，超出村界（含相邻村区域，NaN 5988 中一部分），加剧"未匹配"虚高。

#### 五、结论

- 链路跑通，输出A（套合到外部地块）对玉米准（82.5%），水稻待优化。
- 输出B"对方漏给"判定需先加「非耕地过滤」（用地分类或植被规则）才可靠；ROI 应由 bbox 改为村界精确裁剪。

**产出**：3 个脚本 + `slic_objects_qianjincun.gpkg` + `前进村_耕地地块.gpkg` + 前进村输出A/B + 本节。

### 13.15 用地分类模型核实 + 方案A落地（耕地/非耕地过滤器）（09-02）

#### 一、水稻识别率低的原因（回应"水稻特征明明明显"）

- 淹水特征**有**（`PHENO_flooding` = 第一期 LSWI > NDVI），但**只用了 D04（5月上）一期**，时相窗口窄、单时点脆弱，易与各地实际移栽时间错位。
- LSWI 用 B11（20m SWIR）在 10m 尺度重采样，小地块（13px）噪声大。
- `NDMI` 与 `LSWI` 公式完全重复（都是 `(nir-swir)/(nir+swir)`）；真正对水体更敏感的 `MNDWI=(green-swir)/(green+swir)` 反而不在特征里。
- 前进村（江油/绵阳）不在 23 县训练库（遂宁/泸州/雅安/宜宾），跨区域域差。

> 提升方向：加密移栽期（4 月底~5 月中多旬）＋ 多期淹水信号 ＋ 加 MNDWI ＋ 补江油区域水稻样本。

#### 二、用地分类模型核实（回应"shp 套合哨兵"疑问）

- 哨兵套合**确实做了**：`gee_extract_landuse.py` 提取 6 村哨兵波段 → `gee_6村_哨兵波段_202507.csv`；`landuse_train_fusion.py` 做「无人机 RGB + 哨兵波段/指数」融合训练并评估四大类（83.4%）。
- 但**磁盘保存的 2 个 pkl**（`landuse_6village_model.pkl` / `landuse_qianjin_model.pkl`）都是纯无人机 RGB 特征（18 个 R/G/B/ExG/NGRDI/...），不含哨兵波段。
- **缺口**：`landuse_train_fusion.py` 只打印评估、**未保存融合模型 pkl**，落地产物停在纯 RGB 版。
- 结论：即使补融合模型，仍依赖无人机 RGB 纹理，纯哨兵交付场景用不了 → 方案A 用「纯哨兵过滤器」方向正确。

#### 三、方案A 落地（纯哨兵「耕地/非耕地」过滤器）

```
SNIC 分割 → [耕地/非耕地过滤器(纯哨兵)] → 只对耕地对象做水稻/玉米二分类 → 套合
```

- 用前进村 dltb 真实地类（旱地/水田/水浇地/后备耕地=耕地；林地/园地/草地/建筑/道路/水域=非耕地）＋ 哨兵关键3旬特征，训练 LightGBM 二分类。
- 插入 SLIC 链路，过滤非耕地对象，消除输出B「对方漏给」的林地误判虚高。

**产出**：本节。

### 13.16 方案A落地执行：过滤器训练 + 接入链路 + 前进村前后对比（09-02）

#### 一、过滤器训练结果（`train_cropland_filter.py`）

- 标签：前进村 dltb 有标签超像素 耕地=2387 / 非耕地=6170（NaN 7406 已剔除）。
- 特征：纯哨兵关键3旬（D04/D11/D14）× 10 波段 → `compute_feature_matrix` 154 特征。
- 5 折 CV：**Acc=0.8644，F1(weighted)=0.8675**。
- 混淆矩阵（行=真实 0非耕地/1耕地，列=预测）：`[[5385, 785],[375, 2012]]`
  - 耕地   recall=0.843，precision=0.719（n=2387）
  - 非耕地 recall=0.873，precision=0.935（n=6170）
- Top 特征：D04_B12、DELTA_NDVI、TSTAT_B12_std、DELTA_NDWI、D11_B02、TSTAT_LSWI_std、EVI_D14…

#### 二、接入 SLIC 链路（`predict_slic_overlay.py`）

- 新增 `--filter` 参数 + `apply_cropland_filter()`，但**不改变输出A**：输出A 始终用纯二分类结果；过滤器只给每个超像素附加 `is_crop` 列（1=耕地/0=非耕地）。
- 输出B `judge()` 判定「未匹配-疑似甲方漏给」时：只有 `is_crop==1` 且二分类为水稻/玉米，才报「甲方漏给」；`is_crop==0` 的未匹配对象归为「未匹配(非作物)」。

#### 三、前进村前后对比（外部 shp = 386 块耕地）

| 指标 | 无过滤 | 过滤仅用于B（最终） | 变化 |
|---|---|---|---|
| 输出B「未匹配-疑似甲方漏给」 | 9199 | 1985 | **-78.4%** |
| 输出B「未匹配(非作物)」 | 2814 | 10028 | +7214 |
| 输出A 玉米 | 289 | 289 | 0 |
| 输出A 水稻 | 60 | 60 | 0 |
| 输出A 非耕地 | 0 | 0 | 0 |
| 输出A 不确定 | 33 | 33 | 0 |

> 结论：输出A 完全不受过滤器影响（甲方耕地地块不被误伤），输出B「甲方漏给」虚高从 9199 降到 1985。

#### 四、发现的问题与解决办法

- **问题1（脚本 bug）**：`train_cropland_filter.py` 首版把 SLIC 对象主键当 `fid`，实际 gpkg 主键列是 `label`，报 `KeyError: 'fid'`。已改为 `label`，并对 sjoin 结果去重 `~j.index.duplicated(keep='first')`。
- **问题2（过滤器 trade-off）**：过滤器若同时作用于输出A，会把 68 块（17.6%）甲方耕地地块整体判成「非耕地」（根因：过滤器「耕地」recall 仅 84.3%）。**已按用户决定解决：过滤器仅用于输出B 质检，输出A 不启用过滤器。**

**产出**：`cropland_filter_qianjincun.pkl`、`train_cropland_filter.py`、`predict_slic_overlay.py`（已加 `--filter`，过滤器仅作用于输出B）、`前进村结果/前进村_过滤仅B_{A_赋值,B_质检}.gpkg/csv`。

### 13.17 水稻识别率提升 + 本地样本补江油 + 两套 shp 产出（09-02）

#### 一、特征工程改进（`common.py`）

- **加 MNDWI 水体指数**：`MNDWI=(green-swir1)/(green+swir1)`，对开阔水面/移栽期浅水更敏感，弥补 LSWI/NDMI（二者同式，仅对 NIR 敏感）的不足。VI 列表 8→9，特征总数 154→163。
- **淹水信号增强**：原 `PHENO_flooding` 只用「第一期 LSWI > NDVI」单指数；改为 `(LSWI > NDVI) | (MNDWI > NDVI)` 联合判断，并新增连续特征 `PHENO_flooding_intensity = max(LSWI-NDVI, MNDWI-NDVI)` 表示淹水强度。

#### 二、本地样本补江油（`train_local_qianjincun.py`）

- 用前进村 dltb 真实地类：水田=水稻(90 图斑)、旱地=玉米(286 图斑)，在 SLIC 超像素上质心套合打标签。
- SLIC 级样本：水稻(水田)=658、玉米(旱地)=1689；5 折 CV **Acc=0.8236**，水稻 recall=0.752 / precision=0.664，玉米 recall=0.851 / precision=0.898。
- Top 特征含 MNDWI_D04 / MNDWI_D11 / PHENO_flooding_intensity，证实新增特征有效。

#### 三、前进村套合结果对比（外部 shp = 386 块耕地）

| 指标 | 23县模型（旧） | 本地模型（新） |
|---|---|---|
| 水田→水稻 recall | 35.6% | **87.8%**（79/90） |
| 旱地→玉米 recall | 82.5% | 78.3%（224/286） |
| 输出A 水稻 | 60 | 121 |
| 输出A 玉米 | 289 | 239 |
| 输出A 不确定 | 33 | 23 |
| 输出B「甲方漏给」 | 1985（23县+过滤） | 1947 |

> 结论：补江油本地样本 + MNDWI + 联合淹水信号，使水稻 recall 从 35.6% → 87.8%（+52pp），解决核心痛点；旱地玉米 recall 略降 4.2pp（82.5%→78.3%），属可接受 trade-off。

#### 四、未做（后续优化）

- **加密移栽期（4月底~5月中多旬重新 GEE 提取）**：本次未做（需重新提取前进村 SLIC + 训练多旬特征，成本较高），当前提升已显著，如还需进一步可再推进。

**产出**：`dachun_binary_qianjincun_local.pkl`、`train_local_qianjincun.py`、`common.py`（加 MNDWI/联合淹水）、`前进村结果/前进村_本地模型_{A_赋值,B_质检}.shp/gpkg/csv`。

### 13.18 输出B 修复：parcel_id 负数 + 非耕地（水体等）过滤（09-02）

#### 一、问题与根因

- **parcel_id 负数**：输出B 的 `parcel_id` 之前取的是 SNIC 原始 cluster id（`label`），其值域约 ±21 亿、约半数（7943/15963）为负（GEE reduceToVectors 的 cluster 标签接近 int32 边界）。用户看到的 `-592601372` 即此。**已改为 `fid+1` 干净序号（1..N）**。
- **水体（水湖）被误判为玉米**：本地模型是水稻/玉米二分类，对非作物对象（水体/林地/建筑）会强制判成作物。用户要求产出时过滤掉非作物、只保留作物判断。

#### 二、修复

- `predict_slic`：`parcel_id = fid + 1`（不再用 `label`）。
- `overlay_assign` 输出B：物理过滤——`is_crop==0` 的对象（水体/林地/建筑等非耕地）直接删除，输出B 只保留作物相关对象（`is_crop==1`）。

#### 三、修复后输出B 分布（仅 4909 个作物相关对象，非耕地已删除）

| suit_status | 数量 |
|---|---|
| 正常 | 2335 |
| 未匹配-疑似甲方漏给 | 1947 |
| 地块内冲突 | 347 |
| 未匹配(非作物) | 280 |

> 原始 15963 超像素中 11054 个非耕地（水体/林地等）已被物理删除，`parcel_id` 负数归零。

**产出**：`predict_slic_overlay.py`（parcel_id + 输出B 物理过滤非耕地）。

**最终交付文件位置（前进村，本地模型 + 非耕地过滤，09-02 确认版）**：

- 输出A（主交付：甲方耕地地块 + 作物标签 + 11 质量字段）：
  - `前进村结果\前进村_本地模型_v3_A_赋值.shp`（及 `.gpkg` / `.csv`）
- 输出B（质检附件：作物相关对象 + 套合三类问题，非耕地已物理过滤）：
  - `前进村结果\前进村_本地模型_v3_B_质检.shp`（及 `.gpkg` / `.csv`）
- 模型与脚本：
  - `dachun_binary_qianjincun_local.pkl`（本地水稻/玉米二分类）
  - `cropland_filter_qianjincun.pkl`（耕地/非耕地过滤器）
  - `train_local_qianjincun.py` / `train_cropland_filter.py` / `predict_slic_overlay.py` / `common.py`
