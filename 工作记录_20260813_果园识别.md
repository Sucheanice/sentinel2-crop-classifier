# 工作记录 — 果园识别（跨县迁移学习）

**日期**: 2026-08-12 ~ 08-13

---

## 一、任务背景

新增果园识别需求：对遂宁市（重点安居区）进行果树（不区分树种）识别。采用**跨县迁移学习**策略：用泸州市叙永县的农业保险果树地块（李子+柑橘，276 个地块）作训练标注，训练后迁移推理到遂宁。

与作物识别的区别：作物识别有遂宁本地海量标注（上万地块），果园识别在遂宁**没有本地标注**，只能靠叙永迁移。

---

## 二、阶段演进

| 版本 | 模型 | 季节 | 特征 | CV F1 | 遂宁果树面积 | 斑块数 |
|------|------|------|------|-------|-------------|--------|
| v1 | RF | 3季（缺夏） | 52 | 0.41 | ~3,058 ha | 33,266 |
| v2 | LightGBM | 3季（缺夏） | 52 | 0.37 | 459,272 ha | 80,245 |
| v3 | LightGBM | **4季（含夏）** | 68 | 0.40 | 304,705 ha | 294,322 |

> 结论：**光换算法 + 补夏季数据，F1 基本没变（0.41→0.40）**，但预测结果严重过激（判出遂宁近一半面积是果园）。

---

## 三、夏季 S2 下载的波折（08-12~13）

补充遂宁夏季影像（6-8 月低云）是本次工作的核心，但过程异常曲折：

| 渠道 | 结果 | 原因 |
|------|------|------|
| Planetary Computer COG | 失败 | Azure blob DNS 国内不可达，VPN 带宽不够 |
| AWS S3 JP2（7/26 景） | 失败 | 该景 JP2 解码后 90% 像素为零值（影像本身不完整） |
| AWS S3 JP2（**8/28 景**） | **成功** | 100% 有效像素，10 波段齐全 |

**最终数据**：遂宁 48RWU 瓦片，2025-08-28（S2B，云量 21.8%），10 波段 JP2，由用户在 `E:\迅雷下载\08121721` 手动下载。

**关键修复**：JP2 处理时需将 0 值转为 NaN 再 reproject，否则双线性插值被 0 值污染；写 GeoTIFF 时 NaN 需转 -9999（GDAL 不支持 NaN nodata）。

---

## 四、v3（LightGBM 4季）结果

### 4.1 训练
- 训练区：叙永 4 季（late_autumn / winter / spring / summer 2025-07-16）
- 特征：68 维（4季×10光谱 + 4季×4VI + 3组物候差 + 4纹理 + 2DEM）
- 4-fold 空间分组 CV F1 = **0.40 ± 0.03**
- 样本：正 93,453 / 负 918,642（1:9.8 不平衡）

### 4.2 特征重要性（TOP 10）

| 排名 | 波段 | 重要性 | 说明 |
|------|------|--------|------|
| 1 | band_66 | 1126 | **DEM elevation（绝对主导）** |
| 2 | band_67 | 404 | **DEM slope** |
| 3 | band_38 | 337 | 光谱（spring B08 附近） |
| 4 | band_65 | 294 | 纹理 |
| 5 | band_24 | 274 | 光谱 |

> **关键发现**：模型本质靠地形（高程+坡度）区分果树，而非光谱。这说明叙永→遂宁的光谱可迁移性差，模型学到的"果园"特征主要是"某个海拔+坡度区间"。

### 4.3 推理结果（异常）

- 遂宁全境果树面积：**304,705 ha**（遂宁总面积约 53 万 ha，判出近 58% 是果园，严重异常）
- 斑块数：294,322 个（大量散碎三角）
- 安居区裁切后：3,520 斑块，59,299 ha（安居区总面积约 12.6 万 ha，判出 47% 是果园）

---

## 五、问题诊断

### 5.1 核心问题
LightGBM 4季模型**严重过激**，把遂宁大片丘陵农田误判为果园。根因：
1. 模型被 DEM 主导，遂宁丘陵农田与叙永山地果园落在同一海拔/坡度区间
2. 逐像素预测 + 直接矢量化，产生大量散碎斑块和超大异常斑块（有 129 km² 的斑块，几乎等于整个安居区）
3. 缺少后处理：无 NDVI 校验、无置信度分级、无连通域去噪

### 5.2 与作物识别对比的差距
大春作物识别有"多值判断"机制（ndvi_flag / conf_flag / prdct_cf / max_prob），果园识别只有 `prob>=0.5` 一刀切，鲁棒性差。

---

## 六、后处理改造（已完成）

不重训模型，只在下游推理后加后处理（`postprocess_orchard.py`）：

1. **NDVI 校验**：提取 `vi_summer_ndvi`（8/28 景），果树常绿 NDVI 高、大春作物 8 月底已收获 NDVI 低，阈值 `summer_ndvi < 0.45` 强制判非果园
2. **置信度分级**：`prob ≥ 0.7` 高置信 / `0.5~0.7` 低置信（不确定）/ `< 0.5` 非果园
3. **连通域去噪**：binary_opening 去孤立点 + ndimage.label 连通域标记 + 移除 < 0.3 ha 碎斑
4. **矢量化带字段**：输出 `area_ha / max_prob / mean_prob / ndvi_summer / conf_flag / prdct_lbl`，再裁切安居区

### 6.1 后处理结果

| 范围 | 处理前 | 处理后 | 变化 |
|------|--------|--------|------|
| 安居区 | 59,299 ha（3,520 斑块） | **4,985 ha（1,361 斑块）** | ↓ 92%，相对合理 |
| 全遂宁 | 304,705 ha | 288,133 ha（50,022 斑块） | ↓ 5%，仍严重过激 |

**后处理各环节消减量**：
- NDVI 校验：强制去除非果园 41,012,759 像素（38.7%）
- 二值化（prob≥0.5）：剩 16,709,254 像素（15.78%）
- 连通域去噪（<0.3ha 碎斑）：再去除 4,042,854 像素，剩 12,666,400 像素（11.96%）

### 6.2 结论

- 后处理**能救安居区**（占比 47%→4%），但**救不了全遂宁**（仍 54% 占比）
- **98.6% 是"高置信"**：模型不是"不确定"，而是"自信地错"，因 DEM 高程/坡度给了极强伪证据
- 根子仍是模型 F1=0.40、被 DEM 主导、跨县光谱迁移失效

## 七、产出文件

| 文件 | 说明 |
|------|------|
| `果树识别_step1_coldstart/outputs/shapefile_xuyong_model/orchard_patches.shp` | v3 全遂宁斑块（294,322 个，过激，不建议使用） |
| `果树识别_step1_coldstart/outputs/anju/orchard_anju_dom.shp` | v1 旧版安居区（73 斑块，保守） |
| `果树识别_step1_coldstart/outputs/anju/orchard_patches_anju.shp` | v3 安居区裁切（3,520 斑块，过激） |
| `果树识别_step1_coldstart/outputs/shapefile_xuyong_model/orchard_patches_post.shp` | **v3.1 后处理全遂宁**（50,022 斑块，仍过激） |
| `果树识别_step1_coldstart/outputs/anju/orchard_patches_anju_post.shp` | **v3.1 后处理安居区**（1,361 斑块，4,985 ha，相对合理） |
| `果树识别_step1_coldstart/models/lgb_xuyong_fruit.pkl` | v3 LightGBM 模型 |

---

## 八、v4（补叙永 SAR + 5月 S2）— 08-13 后续

### 8.1 数据补全

迁移学习要求 SAR / 季节特征在**训练侧（叙永）和推理侧（遂宁）同时存在**，因此补齐：

| 数据 | 训练侧（叙永） | 推理侧（遂宁） |
|------|--------------|--------------|
| S1 SAR（9月） | 2025-09-17（rel55 升轨） | 2025-09-29（已有） |
| S1 SAR（11-12月） | 2024-12-03（rel55 升轨） | 2024-11-21（已有） |
| S2 5月（late_spring） | 2025-05-12（云量 1.3%） | 2025-05-20（已有） |

- S1 → dB：`process_xuyong_s1_db.py` 产出 4 个 tif（vv/vh × 2 期），覆盖 100%
- 修复遂宁 5/20 波段顺序 bug（原 B08/B05 写反）

### 8.2 v4 结果

| 指标 | v3（4季） | v4（5季+SAR） |
|------|-----------|----------------|
| CV F1 | 0.40 | **0.4199 ± 0.0333** |
| 波段数 | 68 | 88 |
| 遂宁果树面积 | 304,705 ha | 350,625 ha（199,900 斑块） |

- F1 提升约 2 个点，**主要来自 5月 S2**（`vi_late_spring_ndvi`、`s2_late_spring_B03` 进 TOP10）
- **SAR 贡献有限**（TOP10 无 SAR 波段）
- 遗留隐患：遂宁侧 DEM 缺失填 0，而 DEM 是模型最强特征（训练/推理不一致）

### 8.3 输出

| 文件 | 说明 |
|------|------|
| `outputs/orchard_xuyong_model_suining.tif` | v4 二值 |
| `outputs/orchard_xuyong_model_suining_prob.tif` | v4 概率 |
| `outputs/shapefile_xuyong_model/orchard_patches.shp` | v4 全遂宁斑块（EPSG:32648） |
| `outputs/anju/orchard_anju_dom_v4.shp` | v4 安居区（重投影 EPSG:4523，583 斑块 569 ha） |

---

## 九、G 盘数据勘察 + 大春标注库 + 验证影像（08-13）

### 9.1 G 盘汇交数据勘察

`G:\20260810保险项目成果数据汇交` = 保险项目成果数据（人保+平安；泸州/遂宁/雅安/宜宾，23 县）。

- 全部矢量都是「**大春**」（玉米/水稻/高粱），**无小春**
- 果园：仅叙永水果 276 块专门标注 + 零星约 250 块散落在各县大春数据中

### 9.2 大春训练标注库（合并 23 县）

90,301 块，类别分布：玉米 52,564 / 水稻 34,957 / 高粱 2,154 / 果树 527 / 经济林 61 / 蔬菜 38。

| 文件 | 位置 | 字段 |
|------|------|------|
| `大春训练标注库.gpkg/.shp` | `待训练数据大春\` | 精简 6 字段 + 类别 |
| `大春训练标注库_完整字段.gpkg/.shp` | 项目根目录 | 24 原始字段 + 类别 |

### 9.3 验证影像拷贝

`待训练数据\DOM\射洪大春验证影像\`（均为 ~8cm 无人机级高清 DOM，EPSG:4326）：

| 影像 | 大小 | 覆盖 |
|------|------|------|
| 射洪市14.tif | 1.94 GB | 东部（105.44~105.46E, 31.03~31.05N） |
| 射洪市4.tif | 0.81 GB | 西北部（105.32~105.34E, 31.11~31.13N） |

> G 盘矢量已完整备份至 E 盘，影像仅拷贝射洪 2 块验证用，其余区县 DOM 未拷（G 盘可拔）。
